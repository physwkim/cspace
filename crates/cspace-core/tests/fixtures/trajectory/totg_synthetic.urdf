<?xml version="1.0"?>
<!--
  Not a vendored robot: a minimal purpose-built fixture for
  totg_robot_trajectory_parity.rs, covering two things no real fixture in
  this workspace exercises through the TimeOptimalTrajectoryGeneration
  adapter (round 4's report named both as verified by code inspection
  only): a multi-DOF *active* joint in a group (`planar_group`, a single
  `planar` joint contributing 3 variables), and a group whose active
  joints mix revolute and prismatic types (`mixed_group`), which
  `hasMixedJointTypes`/`has_mixed_joint_types` exists to flag. Kept under
  tests/fixtures/, not the repo-root fixtures/ directory, so
  tools/ci/verify-fixture-provenance.sh's vendored-source mapping (which
  only covers the real panda/fanuc/pr2/dual_arm_panda robots) has no
  reason to know about it — see
  crates/cspace-collision/tests/fixtures/octree_world_robot.urdf for the
  established precedent of this pattern.

  No <limit> on planar_joint: cspace_core::model's URDF loader never reads
  bounds for a multi-DOF joint from URDF (`JointModel::new_planar`
  always constructs unbounded velocity/acceleration, matching upstream's
  own `RobotModel::buildJointInfo`, which likewise never calls
  `jointBoundsFromURDF` — a 1-DOF-only function — for planar/floating
  joints); both fixture groups are driven through
  `compute_time_stamps_with_limits`, so no fixture-side velocity/
  acceleration limit is needed at all.
-->
<robot name="totg_synthetic">
  <link name="base"/>

  <link name="planar_link"/>
  <joint name="planar_joint" type="planar">
    <parent link="base"/>
    <child link="planar_link"/>
    <axis xyz="0 0 1"/>
  </joint>

  <link name="mixed_prismatic_link"/>
  <joint name="mixed_prismatic_joint" type="prismatic">
    <parent link="base"/>
    <child link="mixed_prismatic_link"/>
    <axis xyz="1 0 0"/>
    <limit lower="-1.0" upper="1.0" velocity="1.0" effort="10.0"/>
  </joint>

  <link name="mixed_revolute_link"/>
  <joint name="mixed_revolute_joint" type="revolute">
    <parent link="mixed_prismatic_link"/>
    <child link="mixed_revolute_link"/>
    <axis xyz="0 0 1"/>
    <limit lower="-3.14" upper="3.14" velocity="1.0" effort="10.0"/>
  </joint>
</robot>
