[package]
name = "moveit-geometry"
description = "Frame transforms and geometric primitives for moveit-rs"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
glamx.workspace = true
moveit-error.workspace = true
moveit-octomap.workspace = true
nalgebra.workspace = true
parry3d-f64.workspace = true

[dev-dependencies]
approx.workspace = true
serde.workspace = true
serde_json.workspace = true
# `tests/{octree_in_world,octree_shape_query,body_query}_parity.rs`'s pose
# decoding only. This closes a dev-dependency cycle back through
# `moveit-model` (which depends on this crate normally); Cargo permits that
# because the cycle exists only through a dev-dependency edge, never through
# the crate's own `[dependencies]`.
moveit-test-support.workspace = true

[lints]
workspace = true
