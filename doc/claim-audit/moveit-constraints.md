# Claim audit — moveit-constraints

Per `PORTING-PLAN.md` §175: one row per claim, appended as found, not
batched at report time.

| where | claim | verdict | evidence | commit |
|---|---|---|---|---|
| `crates/moveit-constraints/src/joint.rs:85-89` (originally), and the sibling copies in `position.rs`, `orientation.rs`, `visibility.rs` -- `weight <= EPS` returns `Error::Construct` and cites D6 ("surface silent substitution as an error") as the reason for diverging from upstream's normalize-to-`1.0` | Is D6 the correct justification for erroring here, and does upstream actually normalize? | WRONG on both counts (`PORTING-PLAN.md` D14/§199) | Read all four upstream sites verbatim: `kinematic_constraint.cpp:263` (joint), `:450` (position), `:641` (orientation), `:871` (visibility) -- every one is `if (weight <= std::numeric_limits<double>::epsilon()) { RCLCPP_WARN(...); constraint_weight_ = 1.0; } else constraint_weight_ = weight;`, uniformly, including negative weights (`<= epsilon`, not `< 0.0`). D6 governs an *unresolvable lookup* (`Transforms::transform`'s deviation 1: an unanswerable frame name must not silently become identity); a weight of `0.0`/negative is not unanswerable -- it is a value upstream's own constructor gives the meaning `1.0` to. Citing D6 here was a misclassification this project wrote, not a defensible reading of it | (this row is the finding; the fix is below) |
| `crates/moveit-constraints/src/{joint,position,orientation,visibility}.rs`'s four `new()` constructors | `weight <= EPS` should normalize to `1.0`, matching upstream, rather than returning `Err`; the fix must live in the shared constructor, not a wire-conversion call site, so a Rust caller and a ROS message give the same answer to the same `0.0` input | CONFIRMED, fixed uniformly at all four sites | `weight <= EPS` (unchanged threshold, now including negative weights per upstream's own `<= epsilon`) shadows to `let weight = if weight <= EPS { 1.0 } else { weight };` instead of returning `Err`. Boundary cases tested per site (`0.0`, `f64::EPSILON`, a value just above `EPS`, `-1.0`) in `tests/decide.rs`'s four `new_normalizes_non_positive_weight_to_one` tests -- `0.0`/`EPS`/`-1.0` all normalize to `1.0`, a value just above `EPS` passes through unchanged. All four constructors' doc comments corrected: the wrong D6 citation replaced with a "Weight normalization" section stating the D6/D14 boundary explicitly (unresolvable lookup stays `Err`; a wire default upstream gives meaning to follows upstream) | `c303231` |
| `ros/moveit-ros/src/constraints/{joint,position,orientation,visibility}.rs`'s `TryFrom<*Msg>` impls, `set.rs:50,57`'s aggregate conversion -- claimed live defect: a ROS message's `weight` defaults to `0.0` when unset, and the port rejected such messages outright where upstream builds a working constraint | Does the wire-conversion layer need its own code change, or does the core-crate fix above already close it? | Already closed by the core-crate fix; no `ros/moveit-ros` code change needed | Read all four `TryFrom<*Msg>` impls: each forwards `msg.weight` straight into the corresponding core constructor's `weight` parameter with no separate check of its own (`grep -n weight` on all four files shows only the pass-through and the reverse `weight: c.weight()`). Once the core constructor normalizes instead of erroring, every wire path normalizes too, by construction -- not by a second, parallel fix that could drift from the first. Not build-tested on this host: `ros/moveit-ros` is deliberately outside the root workspace (its own `Cargo.toml`'s D5 comment) because `r2r` requires a local ROS 2 install this host does not have; p9-ros owns that crate's own test/build verification and is writing the wire-path regression test this round | n/a (no `ros/moveit-ros` commit from this crate's finding) |
| `crates/moveit-constraints/src/visibility.rs`'s `normalize_criterion` (pre-fix), shared by `target_radius`/`max_view_angle`/`max_range_angle` | Does upstream apply the same `fabs()` treatment to all three `VisibilityConstraint` criteria, or only to `target_radius_`? | WRONG: the port applied `.abs()` uniformly; upstream does not | Read both upstream sites verbatim: `kinematic_constraint.cpp:818` is `target_radius_ = fabs(vc.target_radius)` before the `> eps` activation gate (`:1081`), so a negative wire value still activates the criterion at its magnitude. `:879-880` assigns `max_view_angle_`/`max_range_angle_` straight from the message with **no** `fabs()` before their own `> eps` gates (`:1081`, `:1109`), so a negative wire value fails that gate and the criterion stays inactive -- upstream itself treats the two shapes differently, it is not a single pattern. The port's one shared `normalize_criterion` helper applied `.map(f64::abs)` to all three, silently flipping an upstream-inactive negative `max_view_angle`/`max_range_angle` into an active positive-magnitude threshold. No upstream `RCLCPP_WARN`/`RCLCPP_ERROR` exists at either site -- this defect carries no textual anchor and was found only by reading the two assignments side by side, not by any `rg` sweep | (fix below) |
| `crates/moveit-constraints/src/visibility.rs`'s `VisibilityConstraint::new` | Split `normalize_criterion` into `normalize_target_radius` (keeps `.abs()`) and `normalize_angle_criterion` (drops it), matching upstream's asymmetry | CONFIRMED, fixed | New test `negative_target_radius_activates_at_its_magnitude_but_negative_angles_stay_inactive` (`tests/decide.rs`) constructs a `VisibilityConstraint` with `target_radius: Some(-0.5)`, `max_view_angle: Some(-0.1)`, `max_range_angle: Some(-0.2)`: asserts `target_radius() == Some(0.5)` (activates at magnitude) and `max_view_angle()`/`max_range_angle() == None` (stay inactive), plus `enabled()` true (target_radius alone suffices). Probe-verified: temporarily restored the old uniform-abs body in `normalize_angle_criterion`, confirmed the test reddened (`left: Some(0.1), right: None`), reverted. `ros/moveit-ros/src/constraints/visibility.rs`'s wire mapping is a pure pass-through (`Some(msg.target_radius)` etc.) with no separate check of its own, so this fix propagates to the wire path automatically -- no `ros/moveit-ros` change needed | `0ca8916` |
| `crates/moveit-model/src/robot_model.rs`'s `RobotModel::group_joint_roots`/`group_updated_links`, exercised by the "cross"/"hand"/"other" test fixture groups (all built entirely from `type="fixed"` joints) | Is `updated_link_names() == []` for those three fixture groups upstream's own behaviour, a degenerate fixture, or a real defect in the updated-link computation? Routed unfixed from p3-distance-field as "latent, not currently vacuous" | Upstream's own behaviour, not a defect | Read `joint_model_group.cpp:139-153`: only a joint with `getVariableCount() > 0` is admitted to `active_joint_model_vector_`; a fixed joint (`vc == 0`) always lands in `fixed_joints_` instead (`:176-177`). `:182-187` seeds `joint_roots_` solely from `active_joint_model_vector_`; `:256-260` fills `updated_link_model_set_` solely by descending from `joint_roots_`. A group made entirely of fixed joints therefore has empty `joint_roots_` and an empty updated-link set upstream too -- `RobotModel::group_joint_roots`/`group_updated_links` already reproduce this exactly (`active_joint_indices()`-seeded, same as upstream). No existing test asserted the boundary directly (the three fixtures exercise chain-topology and end-effector-parent resolution, and never read `updated_link_names()` themselves), so added `group_of_only_fixed_joints_has_no_joint_roots_or_updated_links` as a minimal dedicated fixture for the boundary itself, rather than relying on the three incidental ones | `1340757` |
| `crates/moveit-constraints/src/joint.rs:188-191` (bounds clamp + `tolerance=EPS`), `visibility.rs:265` (`cone_sides.max(3)`), `visibility.rs:266` (`normalize_target_radius`'s `.abs()`) -- three prior claim-audit rows marked "already correctly ported" from reading, with no cited test | For each, name the test that reddens if the substitution is dropped, or write one | CONFIRMED: all three already had a demonstrating test; none needed writing | Probe-verified all three by temporarily reverting the substitution and confirming the named test fails, then reverting: (1) `joint.rs`'s bounds-clamp branch replaced with a no-op passthrough -- `new_clamps_a_position_outside_the_joint_bounds` (`tests/decide.rs:191`) reddened on `desired_joint_position()`. (2) `visibility.rs`'s `cone_sides.max(3)` replaced with raw `cone_sides` -- `cone_sides_below_three_is_clamped` (`:953`) reddened, `left: 1, right: 3`. (3) `normalize_target_radius`'s `.map(f64::abs)` dropped -- `negative_target_radius_activates_at_its_magnitude_but_negative_angles_stay_inactive` (`:904`) reddened, `left: None, right: Some(0.5)` | n/a (no code change; verification only) |
| `crates/moveit-constraints/src/position.rs`'s shape/mesh-construction-failure hard-error (`kinematic_constraint.cpp:401-419`/`429-446`'s skip-one-continue), `orientation.rs`'s empty-`frame_id` hard-error (`:618-619`'s warn-only-continue) -- two "distinct, skip" rows resting on a doc comment, not a cited test | Show the test that proves the port hard-errors, not the doc comment saying it does | CONFIRMED, both needed a new test; both now written and probe-verified | `PositionConstraint`'s array-length-mismatch half of the family (`primitive_poses.size() <= i`) is type-excluded, not testable -- `ConstraintRegion` pairs `(Body, Isometry3)` in one `Vec`, so mismatched lengths cannot be constructed. The shape/mesh-construction-failure half is real and untested: added `new_rejects_a_shape_with_no_body_counterpart` (`tests/decide.rs`), covering `Shape::Cone` and `Shape::Plane`. The upstream mapping in this row's first version was wrong and is corrected here: `:401-419`'s two warn-and-`continue` skips are a short pose array (`:400-403`) and `constructShapeFromMsg` returning null on a malformed message (`:417-418`), both type-excluded in this port. The branch a bodyless shape actually reaches is `:412-413`, which feeds `createEmptyBodyFromShapeType(shape->type)` straight into `body->setDimensionsDirty(shape.get())` unguarded -- upstream dereferences null there rather than skipping. Reachable, not theoretical: `constraint_region.primitives` is a `shape_msgs/SolidPrimitive[]` and `SolidPrimitive::CONE` is one of the four types `constructShapeFromMsg` builds (`shape_operations.cpp:101-106`), with no filter in between, so a cone region crashes upstream MoveIt. `Shape::Plane`/`Shape::OcTree` take the same port-side branch but cannot arrive on that upstream path at all -- probe-verified by temporarily replacing the `?`-propagating construction with upstream's skip-continue, confirming the constraint built successfully with an empty region list instead of erroring. For orientation, added `new_rejects_empty_frame_id` (distinct from the existing `new_rejects_unresolvable_frame`, which only covers a non-empty unresolvable name -- upstream branches on `.empty()` specifically at `:618`, separately from its general frame resolution) -- probe-verified by temporarily letting an empty `frame_id` bypass the reject check, confirming the constraint built successfully instead of erroring | `b4a9236`, upstream mapping corrected in the follow-up |
| The row above's cone-crash claim, re-examined at production-reachability standard: is there a validator anywhere between a client's `moveit_msgs::Constraints` and `PositionConstraint::configure` that would reject a `CONE` region before `kinematic_constraint.cpp:412-413`'s unguarded null deref, in MoveGroup, PlanningSceneMonitor, `constructConstraints`, or anywhere else? | Not a synthetic worst-case -- CONFIRMED reachable from an ordinary MoveGroup action client, via two independent, unfiltered production call chains, with no validator anywhere in between | Searched for a validator first, found none: `grep -rln "validateConstraint\|checkConstraint\|sanitizeConstraint"` across all of `/home/stevek/work/moveit2` -- zero hits. Every `moveit_ros/planning/*adapter*.cpp` grepped for primitive/shape/`constraint_region` handling -- zero hits (planning-request adapters touch trajectories and start states, not goal constraint content). Then traced two real chains forward from the goal-acceptance callback, both unfiltered end to end: (1) `move_action_capability.cpp:64-83`'s `rclcpp_action::create_server` goal handler unconditionally `ACCEPT_AND_EXECUTE`s every goal, no content check; `executeMoveCallbackPlanAndExecute` (`:131-154`), on the common `planning_options.planning_scene_diff`-empty path, calls `lscene->isStateConstrained(current_state, mergeConstraints(goal->request.goal_constraints[i], goal->request.path_constraints))` on the raw client request *before planning starts*; `PlanningScene::isStateConstrained(state, msg, verbose)` (`planning_scene.cpp:2253-2258`) builds a fresh `KinematicConstraintSet` and calls `ks->add(constr, getTransforms())` directly on the message, no validation. (2) independently, the actual planning entry point every OMPL request goes through -- `ModelBasedPlanningContext::setGoalConstraints` (`model_based_planning_context.cpp:663-680`) does the identical `mergeConstraints(goal_constraint, path_constraints)` -> fresh `KinematicConstraintSet` -> `kset->add(constr, getPlanningScene()->getTransforms())`, so the crash is on the mainline path, not only the "already satisfied" shortcut. `mergeConstraints` (`utils.cpp:120-130`) is unconditional concatenation for `position_constraints`/`orientation_constraints`/`visibility_constraints` -- only `joint_constraints` gets per-joint merge logic (`:69-104`), so a CONE region survives the merge unmodified. `KinematicConstraintSet::add(vector<PositionConstraint>, tf)` (`kinematic_constraint.cpp:1246-1260`) calls `configure()` on every element with no shape-type filter. Both chains land on the same `:412-413` unguarded null deref this row already established. Corrects nothing in the row above -- it strengthens it: two independent unfiltered production paths, not one | n/a (doc-only; no port code changed by this row) |
| Directory-level unported-file audit: does `kinematic_constraints/` or `constraint_samplers/` (`moveit_core`) contain a file this crate never cites, that its own doc comments' claim of completeness would not surface? | 0 missing files in both directories; `constraint_samplers/` already carries its own exhaustive per-symbol audit inside `lib.rs`, re-verified this round | `find moveit_core/{kinematic_constraints,constraint_samplers} -name '*.cpp' -o -name '*.hpp'` (`.h` shims excluded, same convention as the `moveit-model` row): `kinematic_constraints/` is `kinematic_constraint.hpp`+`.cpp` (ported: `joint/position/orientation/visibility/set.rs`) and `utils.hpp`+`.cpp` (ported: `utils.rs`) -- both already carry the "`## kinematic_constraint.hpp`/`utils.hpp`" per-declaration mapping at `lib.rs:122-311`; `test/*.cpp` (2 files) are upstream's own unit tests, not public API, superseded by this crate's own oracle-fixture tests. `constraint_samplers/` is six `.hpp`/`.cpp` pairs (`constraint_sampler`, `constraint_sampler_allocator` (header-only), `constraint_sampler_manager`, `constraint_sampler_tools`, `default_constraint_samplers`, `union_constraint_sampler`) plus `test/pr2_arm_ik*`/`pr2_arm_kinematics_plugin*` (upstream's own IK-plugin test fixtures, not public API) -- all six cited in this crate, and `lib.rs:598-599` already carries a self-reproducing count (`rg -c '^//! - CS:' lib.rs` == 66, one row per public declaration across all six headers), re-run this round and still 66. Not a fresh audit invalidating that one -- confirms it still holds and closes the "was this ever checked at the directory level, not just symbol level" question the file-count method exists to catch | n/a (doc-only; no code change, no gap found) |
