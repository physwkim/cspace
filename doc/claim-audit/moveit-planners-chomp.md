# Claim audit — `moveit-planners-chomp`

Per `PORTING-PLAN.md` §175: one row per claim about upstream behaviour
found in this crate, verified against the actual upstream source (not
inferred from the port), appended as found rather than batched at
report time.

| where | claim | verdict | evidence | commit |
|---|---|---|---|---|
| `src/planner.rs`, `solve`'s recovery loop / `validate_recovery_time_limit` doc | upstream implicitly (no `static_cast`) narrows `planning_time_limit_ + 5` (a `double`) into `setRecoveryParams`'s `int planning_time_limit` parameter — undefined behaviour in C++ outside `int`'s range, silently saturating/overflowing under a naive Rust `as i32` transcription | CONFIRMED | `moveit_planners/chomp/chomp_motion_planner/src/chomp_planner.cpp:177-181` (the call site, no cast) and `moveit_planners/chomp/chomp_motion_planner/include/chomp_motion_planner/chomp_parameters.hpp:58` (`setRecoveryParams`'s declared `int planning_time_limit` parameter) — both read directly | `2d54e7d` |
| `src/trajectory.rs`, `fill_in_from_trajectory`'s `prev_idx`/`fraction` narrowing | this Rust site's unguarded `f64 -> usize` truncation matches upstream's own equally unguarded `static_cast<double>(...) ; std::trunc(...)` narrowing in `fillInFromTrajectory` — `distinct` from the `§172` `resample_dt`/`discretization` defect family, not the same defect | CONFIRMED (as `distinct`) | `moveit_planners/chomp/chomp_motion_planner/src/chomp_trajectory.cpp:185-213` — read directly | (no fix; classification only) |
| `src/cost.rs`, `ChompCost::new`'s dropped `joint_number` parameter | upstream's `ChompCost(const ChompTrajectory&, int joint_number, const std::vector<double>&, double)` never reads `joint_number` (marked `/* joint_number */`, a comment not a name); its only real call site passes `i` but the constructor never uses it | CONFIRMED (TYPE_A) | `chomp_cost.hpp:52` (signature) and `chomp_cost.cpp:47` (`/* joint_number */`) read directly; call site `chomp_optimizer.cpp:127` (`ChompCost(group_trajectory_, i, derivative_costs, parameters_->ridge_factor_)`) confirms `i` has no effect | (uncommitted) |
| `src/cost.rs`, `ChompCost::new`'s `derivative_costs.len() > DIFF_RULES.len()` guard | upstream loops `for (i = 0; i < derivative_costs.size(); ++i) { ...DIFF_RULES[i]... }` against a fixed `DIFF_RULES[3][DIFF_RULE_LENGTH]` C array with no bound check against 3, so an oversized `derivative_costs` indexes out of bounds | CONFIRMED (TYPE_A) | `chomp_cost.cpp:57-61` (unbounded loop over `derivative_costs.size()`) and `chomp_utils.hpp:49` (`DIFF_RULES[3][...]`) read directly | (uncommitted) |
| `src/cost.rs`, `ChompCost::new`'s `num_points() < 2*(DIFF_RULE_LENGTH-1)` guard | upstream computes `num_vars_free = num_vars_all - 2*(DIFF_RULE_LENGTH-1)` as a plain `int` with no lower-bound check, then passes it as a block size to `Eigen::MatrixXd::block()` — negative on too-short a trajectory, an `eigen_assert` in debug/UB in release | CONFIRMED (TYPE_A) | `chomp_cost.cpp:50-51` (`num_vars_free` computed unchecked) and `:66` (`.block(DIFF_RULE_LENGTH - 1, DIFF_RULE_LENGTH - 1, num_vars_free, num_vars_free)`) read directly | (uncommitted) |
| `src/cost.rs`, `ChompCost::new`'s singular-`quad_cost` rejection | upstream's `quad_cost_ = quad_cost_.inverse()` (`Eigen::MatrixXd::inverse()`, the generic/non-fixed-size overload) performs no invertibility check at all — Eigen's own doc comment on `MatrixBase::inverse()` states "This matrix must be invertible, otherwise the result is undefined" | CONFIRMED (TYPE_A) | `chomp_cost.cpp:69` (`quad_cost_inv_ = quad_cost_.inverse();`) and `Eigen/src/LU/InverseImpl.h:268` (doc comment, read from `/home/stevek/work/ITK/.../Eigen/src/LU/InverseImpl.h` — vendored Eigen source, no local copy in `moveit2`) | (uncommitted) |
| `src/cost.rs`, `max_quad_cost_inv_value`'s empty-matrix rejection | upstream's `getMaxQuadCostInvValue` calls `quad_cost_inv_.maxCoeff()` unguarded; `maxCoeff()` bottoms out in `DenseBase::redux()`'s `eigen_assert(this->rows() > 0 && this->cols() > 0 && "you are using an empty matrix")`, so a zero-free-variable trajectory is an assert failure (debug) / UB (release) upstream, not a documented return | CONFIRMED (TYPE_A) | `chomp_cost.cpp:92-95` (`getMaxQuadCostInvValue` body) and `Eigen/src/Core/Redux.h:445` (the `eigen_assert`, same vendored-Eigen caveat as above) | (uncommitted) |
| `src/cost.rs`, `derivative`'s owned-`DVector` return (vs. upstream's output-parameter aliasing) | upstream's `getDerivative` is a template writing into a caller-supplied `Eigen::MatrixBase<Derived>& derivative` output view, so the result can alias an existing buffer; this crate's only call site never needs that aliasing | CONFIRMED (TYPE_A) | `chomp_cost.hpp:56-57,78-83` (template signature and body: `derivative = (quad_cost_full_ * (2.0 * joint_trajectory));`) read directly | (uncommitted) |
| `src/cost.rs` module doc, `quad_cost_inv_`'s decomposition-family note | `Eigen::MatrixXd::inverse()` always dispatches through the generic `compute_inverse` specialization (`matrix.partialPivLu().inverse()`), never the closed-form cofactor path, because that path is only selected by `MatrixType::RowsAtCompileTime` being a *compile-time* fixed size <= 4 — `MatrixXd`'s `RowsAtCompileTime` is `Eigen::Dynamic` regardless of runtime size, so it always falls to the general case; Eigen's own doc comment states this in prose ("For small fixed sizes up to 4x4 ... cofactors. In the general case ... class PartialPivLU") | CONFIRMED (TYPE_A) | `Eigen/src/LU/InverseImpl.h:25-30` (`compute_inverse<MatrixType, ResultType, Size = MatrixType::RowsAtCompileTime>` general-case `run()`) and `:265-271` (doc comment) — vendored Eigen source, read directly | (uncommitted) |
| `src/cost.rs`, `diff_matrix`'s boundary truncation | upstream's `getDiffMatrix` truncates (via `continue`) any stencil tap that would land outside `[0, size)`, with no reflection or renormalization at the ends | CONFIRMED (TYPE_B) | `chomp_cost.cpp:74-90` (`if (index < 0) continue; if (index >= size) continue;`, no post-loop renormalization) read directly | (uncommitted) |
| `src/utils.rs`, `DIFF_RULE_LENGTH` value | upstream declares `static const int DIFF_RULE_LENGTH = 7;` | CONFIRMED (TYPE_B) | `chomp_utils.hpp:46` read directly | (uncommitted) |
| `src/utils.rs`, `DIFF_RULES` literal transcription | upstream's velocity row's center coefficient is written `6 / 6.0` (not `1.0`); all three rows' literal fractions match the port's transcription | CONFIRMED (TYPE_B) | `chomp_utils.hpp:49-53` read directly | (uncommitted) |
| `src/utils.rs`, `robot_state_to_array` reads only each joint's first variable | upstream's `robotStateToArray` reads `state.getVariablePosition(jm->getFirstVariableIndex())` per active joint model, with no guard rejecting a multi-DOF active joint (silently drops all but the first variable) | CONFIRMED (TYPE_B) | `chomp_utils.hpp:55-62` read directly | (uncommitted) |
| `src/utils.rs`, `robot_state_to_array`'s typed-error group lookup (vs. upstream's unchecked deref) | upstream's `robotStateToArray` calls `state.getJointModelGroup(planning_group_name)` and immediately dereferences the returned pointer (`group->getActiveJointModels()`) with no null check — a latent null-pointer dereference on an unknown group name | CONFIRMED (TYPE_A) | `chomp_utils.hpp:58-61` read directly | (uncommitted) |
| `src/utils.rs` module doc, angle-normalization functions are a fresh transcription, not shared | upstream's `normalizeAnglePositive`/`normalizeAngle`/`shortestAngularDistance` bodies in `chomp_utils.hpp` are marked `// copied from geometry/angles/angles.h`, upstream's own signal that this is a local copy rather than a shared dependency on the ROS `angles` package | CONFIRMED (TYPE_A) | `chomp_utils.hpp:64-86` read directly | (uncommitted) |
| `src/parameters.rs`, `ChompParameters::default()` matches upstream's default constructor | all 19 default-constructor field values in `ChompParameters::ChompParameters()` (`planning_time_limit_=6.0` through `max_recovery_attempts_=5`) — checked field-by-field against the port's `Default` impl | CONFIRMED (TYPE_B) | `chomp_parameters.cpp:43-67` read directly, compared against `src/parameters.rs:105-126` line-by-line | (uncommitted) |
| `src/parameters.rs`, `set_recovery_params` touches exactly 4 fields | upstream's `setRecoveryParams` body assigns exactly `learning_rate_`, `ridge_factor_`, `planning_time_limit_`, `max_iterations_` and nothing else | CONFIRMED (TYPE_B) | `chomp_parameters.cpp:71-78` read directly | (uncommitted) |
| `src/parameters.rs`, `set_trajectory_initialization_method`'s silent-no-op-on-invalid semantics | upstream's `setTrajectoryInitializationMethod` returns `false` and leaves `trajectory_initialization_method_` unchanged on an invalid method — no exception, no error, a pure boolean-gated no-op | CONFIRMED (TYPE_B) | `chomp_parameters.cpp:83-92` read directly | (uncommitted) |
| `src/parameters.rs`, `VALID_INITIALIZATION_METHODS` value | upstream's `ChompParameters::VALID_INITIALIZATION_METHODS` is exactly `{"quintic-spline", "linear", "cubic", "fillTrajectory"}` | CONFIRMED (TYPE_B) | `chomp_parameters.cpp:80-81` read directly | (uncommitted) |

| `src/trajectory.rs` module doc, `num_free_points`'s double-wraparound note | upstream's `getNumFreePoints` computes `(end_index_ - start_index_) + 1` in `size_t` arithmetic; for a 2-point trajectory (`start_index_=1`, `end_index_=0`) this underflows to `SIZE_MAX` on the subtraction, then overflows back to `0` on the `+1` — correct only because the two wraparounds cancel | CONFIRMED (TYPE_A) | `chomp_trajectory.hpp:240-243` (`inline size_t ChompTrajectory::getNumFreePoints() const { return (end_index_ - start_index_) + 1; }`) read directly; `end_index_=num_points_-2` from `chomp_trajectory.cpp:54` gives `end_index_=0` for `num_points_=2` | (uncommitted) |
| `src/trajectory.rs`, `from_duration` ported from the unchecked `static_cast<size_t>` delegating constructor | upstream's `(robot_model, double duration, double discretization, group_name)` constructor delegates via `static_cast<size_t>(duration / discretization) + 1`, an unguarded cast with no validation of `discretization` | CONFIRMED (TYPE_A) | `chomp_trajectory.cpp:41-45` read directly | (uncommitted) |
| `src/trajectory.rs`, `from_num_points`'s `num_points < 2` guard | upstream's `(robot_model, size_t num_points, ...)` constructor initializes `end_index_(num_points_ - 2)` in `size_t` arithmetic with no lower-bound check, underflowing for `num_points_ < 2` | CONFIRMED (TYPE_A) | `chomp_trajectory.cpp:47-54` (`end_index_(num_points_ - 2)` in the initializer list) read directly | (uncommitted) |
| `src/trajectory.rs`, `from_source_trajectory`'s `start_extra`/`end_extra` computed in `i64` | upstream computes the equivalent `start_extra`/`end_extra` as plain `int` (can go negative), not `size_t` | CONFIRMED (TYPE_B) | `chomp_trajectory.cpp:68-69` (`int start_extra = ...`, `int end_extra = ...`) read directly | (uncommitted) |
| `src/trajectory.rs`, `fill_in_linear_interpolation`'s "`end_index - 1` denominator" quirk | upstream's `fillInLinearInterpolation` divides by `(end_index - 1)`, not the true span `(end_index - start_index)`, for every `start_index`, not only the common `start_index_==1` case — not a port deviation, a transcribed quirk | CONFIRMED (TYPE_B) | `chomp_trajectory.cpp:106-118` (`double theta = ((*this)(end_index, i) - (*this)(start_index, i)) / (end_index - 1);`) read directly | (uncommitted) |
| `src/trajectory.rs`, `fill_in_cubic_interpolation`'s dropped `coeffs[1]` | upstream's `coeffs` is a 4-element vector default-initialized to `0`; `coeffs[1]` (index 1, the linear term) is written nowhere in `fillInCubicInterpolation` and never appears in the evaluated polynomial (`coeffs[0] + coeffs[2]*pow(t,2) + coeffs[3]*pow(t,3)`) | CONFIRMED (TYPE_B) | `chomp_trajectory.cpp:120-140` read directly — `coeffs[1]` set only by the `(4, 0)` constructor, never assigned or read afterward | (uncommitted) |
| `src/trajectory.rs`, `fill_in_min_jerk`/`fill_in_linear_interpolation`/`fill_in_cubic_interpolation`'s unchecked `start_index - 1` | upstream computes `double start_index = start_index_ - 1;` (an unsigned `size_t` promoted through this subtraction) with no guard against `start_index_ == 0`, in all three `fillIn*` methods | CONFIRMED (TYPE_A) | `chomp_trajectory.cpp:108-109` (`fillInLinearInterpolation`), `:122-123` (`fillInCubicInterpolation`), `:144-145` (`fillInMinJerk`) — all three read directly | (uncommitted) |
| `src/trajectory.rs`, `joint_velocities`'s unchecked windowed read | upstream's `getJointVelocities` template loops `trajectory_.row(traj_point + k)` for `k` in `[-DIFF_RULE_LENGTH/2, DIFF_RULE_LENGTH/2]` with no bounds check near either end of the trajectory | CONFIRMED (TYPE_A) | `chomp_trajectory.hpp:292-302` read directly | (uncommitted) |
| `src/trajectory.rs`, `assign_chomp_trajectory_point_from_robot_state`'s two typed-error checks | upstream's `assignCHOMPTrajectoryPointFromRobotState` guards the same two invariants (group active-joint count matches the trajectory's column count; every active joint has exactly 1 variable) only via `assert()`, compiled out in release builds | CONFIRMED (TYPE_A) | `chomp_trajectory.cpp:211-223` (`assert(group->getActiveJointModels().size() == ...)` at `:216`, `assert(jm->getVariableCount() == 1)` at `:220`) read directly | (uncommitted) |
| `src/trajectory.rs`, `fill_in_from_trajectory`'s `Err` on a `None` group (vs. upstream's unchecked null) | upstream's `fillInFromTrajectory` reads `trajectory.getGroup()` (which can return `nullptr`) with no null check, and passes it straight into `RobotState::interpolate` | CONFIRMED (TYPE_A) | `chomp_trajectory.cpp:188-209` (`group` fetched at `:197`, passed unchecked into `.interpolate(..., group)` at `:205`) read directly | (uncommitted) |
| `src/trajectory.rs`, `fill_in_from_trajectory` returns `false` on fewer than 2 waypoints | upstream's `fillInFromTrajectory` returns `false` immediately if `trajectory.getWayPointCount() < 2` | CONFIRMED (TYPE_B) | `chomp_trajectory.cpp:188-192` read directly | (uncommitted) |
| `src/trajectory.rs`, `trajectory_point`/`joint_trajectory` returning owned copies (vs. upstream's live `RowXpr`/`ColXpr` views) | upstream's `getTrajectoryPoint`/`getJointTrajectory` return `Eigen::MatrixXd::RowXpr`/`ColXpr`, live views into `trajectory_` that alias further mutation | CONFIRMED (TYPE_A) | `chomp_trajectory.hpp:86,88,225-233` read directly | (uncommitted) |

| `src/optimizer.rs` module doc, `hy_env_` has exactly 5 references / `getCollisionGradients` is a one-line forward | `hy_env_` appears exactly 5 times in `chomp_motion_planner/` (1 field decl + 4 uses in `chomp_optimizer.cpp`), and `CollisionEnvHybrid::getCollisionGradients` is a single-statement forward to `cenv_distance_->getCollisionGradients(...)` where `cenv_distance_` is a `CollisionEnvDistanceFieldPtr` — this is the load-bearing fact behind round 19 reversing round 17's "permanently unportable" verdict on `ChompOptimizer` | CONFIRMED (TYPE_A) | `chomp_optimizer.hpp:133` and `chomp_optimizer.cpp:76,78,102,890` (5 references, `rg -n hy_env_`), `collision_env_hybrid.cpp:172-177` (one-line forward) and `collision_env_hybrid.hpp:149` (`CollisionEnvDistanceFieldPtr cenv_distance_`) — all read directly | (uncommitted) |
| `src/optimizer.rs` "Precondition" note, `initialize()`'s free-variable-count wiring | upstream's `initialize()` sets `num_vars_free_ = group_trajectory_.getNumFreePoints(); free_vars_start_ = group_trajectory_.getStartIndex(); free_vars_end_ = group_trajectory_.getEndIndex();` immediately after constructing `group_trajectory_` via the padding constructor | CONFIRMED (TYPE_A) | `chomp_optimizer.cpp:90-95` read directly | (uncommitted) |
| `src/optimizer.rs`, `calculate_total_increments`'s weighted-combination formula | upstream's `calculateTotalIncrements` computes exactly `final_increments_.col(i) = learning_rate_ * (quad_cost_inv * (smoothness_cost_weight_ * smoothness_increments_.col(i) + obstacle_cost_weight_ * collision_increments_.col(i)))` | CONFIRMED (TYPE_B) | `chomp_optimizer.cpp:632-641` read directly, matches `src/optimizer.rs:439-441` term-for-term | (uncommitted) |
| `src/optimizer.rs`, `add_increments_to_trajectory`'s unguarded IEEE-754 zero-column scaling | upstream's `addIncrementsToTrajectory` computes `joint_update_limit_ / fabs(max)` / `fabs(min)` with no zero-guard; an all-zero column yields `+inf`, which `if (max_scale < scale)` never selects (`inf < 1.0` is false) | CONFIRMED (TYPE_A) | `chomp_optimizer.cpp:643-661` read directly | (uncommitted) |
| `src/optimizer.rs`, `handle_joint_limits`'s continuous-joint skip and correction formula | upstream's `handleJointLimits` skips continuous revolute joints entirely (`revolute_joint->isContinuous()` → `continue`) and corrects the single worst violation per pass via `multiplier = max_violation / quad_cost_inv(free_var_index, free_var_index)` applied along `quad_cost_inv`'s matching column, up to 10 passes | CONFIRMED (TYPE_B) | `chomp_optimizer.cpp:781-860` read directly | (uncommitted) |
| `src/optimizer.rs` "termination condition" doc, `optimize()`'s overall control flow (loop bound, best-trajectory tracking, 3 early-exit conditions in order, post-loop restore, boolean return) | upstream's `optimize()` loops `iteration_` from `0` to `max_iterations_` (exclusive), tracks `best_group_trajectory_`/`best_group_trajectory_cost_` on iteration 0 or improvement, checks (1) every-10th-iteration mesh check, (2) `!filter_mode_` collision-threshold check, (3) an unconditional wall-clock `break`, in that order; `should_break_out` handling increments `collision_free_iteration_` and breaks past `num_collision_free_iterations_`; restores `best_group_trajectory_` unconditionally after the loop; returns `is_collision_free_` | CONFIRMED (TYPE_B) | `chomp_optimizer.cpp:290-518` read in full, matches `src/optimizer.rs:1452-1548`'s `ChompOptimizer::optimize` line-for-line in control-flow shape | (uncommitted) |
| `src/optimizer.rs` "termination condition" doc, item 1's mesh check operates "against the *current* trajectory (not `best_group_trajectory_`)" | **Backwards.** Upstream's `isCurrentTrajectoryMeshToMeshCollisionFree` builds its `moveit_msgs::msg::RobotTrajectory` entirely from `best_group_trajectory_(i, j)`, never from `group_trajectory_` (the actual current iterate) — the function is misleadingly named but its body reads only the best-seen trajectory. This directly contradicts this same file's own, correct, later claim (`src/optimizer.rs:820-823`: "the check reads `best_group_trajectory_`'s *values*... not the just-`updateFullTrajectory`'d current iterate") and the port's own code, which correctly passes `self.best_group_trajectory` into the closure (`src/optimizer.rs:1503-1504`) | EXPIRED (TYPE_B, self-contradictory/wrong prose — code is correct) | `chomp_optimizer.cpp:520-537` (`isCurrentTrajectoryMeshToMeshCollisionFree`'s body reads only `best_group_trajectory_`) read directly, contradicting `src/optimizer.rs:294`'s "against the *current* trajectory (not `best_group_trajectory_`)" | (uncommitted) |
| `src/optimizer.rs`, `resolve_collision_point_joint_index`/`collision_point_joint_names_` flattening rationale | upstream's `initialize()` writes `collision_point_joint_names_[i][j]` for every `i` in `free_vars_start_..=free_vars_end_`, but the inner value never depends on `i` — it is derived entirely from `gsr_->gradients_`, a single snapshot read once outside the `i` loop, so every written row is identical | CONFIRMED (TYPE_A) | `chomp_optimizer.cpp:222-242` read directly | (uncommitted) |
| `src/optimizer.rs` `ChompOptimizer` doc, dead/write-only fields `group_trajectory_backup_`/`state_is_in_collision_` | `group_trajectory_backup_` is written once in live code (`initialize()`) but every read of it is inside commented-out HMC code; `state_is_in_collision_` is written every `performForwardKinematics` call but has no read anywhere in `chomp_motion_planner/src` | CONFIRMED (TYPE_A) | `chomp_optimizer.cpp:150,441,457` (`group_trajectory_backup_`: live write, both reads commented) and `:166,893,916` (`state_is_in_collision_`: live writes, `rg` finds no read) — read directly | (uncommitted) |
| `src/optimizer.rs` `ChompOptimizer` doc, `point_is_in_collision_`'s "one read is inside a `/* */`-commented block" | **Undercounts.** There are two reads: a live, uncommented one immediately following the assignment (`chomp_optimizer.cpp:914`, in the same statement block that sets `state_is_in_collision_`/`is_collision_free_` — this is the computation the port claims "survives as an inline local `bool`", and it does, correctly), and a separate, genuinely dead commented-out read at `:615`. The claim's literal wording ("its one read is... commented") describes only the dead one and omits the live one that actually matters; the port's resulting *behavior* (inlining the live computation, dropping the field) is still correct | EXPIRED (TYPE_A, imprecise — behavior unaffected) | `chomp_optimizer.cpp:912-920` (live assignment-then-read) and `:615` (the actual commented read, inside `/* ... */`) read directly | (uncommitted) |
| `src/optimizer.rs` `ChompOptimizer` doc, `Isometry3d * Vector3d` point-vs-vector citation | The quoted line `axis = joint_transform * axis;` is real and its point-transform semantics claim (Eigen applies rotation *and* translation to a bare `Vector3d` multiplied by an `Isometry3d`) is correct, but it is at `chomp_optimizer.cpp:749`, not the cited `:733` | EXPIRED (citation line wrong by 16 lines; substance CONFIRMED) | `chomp_optimizer.cpp:749` (`rg -n 'axis = joint_transform \* axis;'`) vs. `src/optimizer.rs:880`'s `chomp_optimizer.cpp:733` citation | (uncommitted) |
| `src/optimizer.rs`, `rsl::uniform_real(0., 1.)` → `rng.random_range(0.0..1.0)` substitution site | upstream's stochastic-descent start point is `static_cast<int>(rsl::uniform_real(0., 1.) * (free_vars_end_ - free_vars_start_) + free_vars_start_)` | CONFIRMED (TYPE_A) | `chomp_optimizer.cpp:566-573` read directly | (uncommitted) |
| `src/optimizer.rs`, `destroy()` not ported | upstream's `destroy()` body is exactly `{ // Nothing for now. }` | CONFIRMED (TYPE_A) | `chomp_optimizer.hpp:68-71` read directly | (uncommitted) |
| `src/optimizer.rs`, `debugCost` confirmed dead | `debugCost` is defined (`chomp_optimizer.cpp:668-674`) but has no call site anywhere in `chomp_optimizer.cpp` | CONFIRMED (TYPE_A) | `rg -n debugCost chomp_optimizer.cpp` (definition + declaration only, no call) | (uncommitted) |
| `src/optimizer.rs`, HMC path (`perturbTrajectory`/`getRandomMomentum`/`updateMomentum`/`updatePositionFromMomentum`) confirmed dead | every call site in `optimize()` is commented out (`perturbTrajectory();` at `:442`, `getRandomMomentum();`/`updateMomentum();`/`updatePositionFromMomentum();` at `:359-361`, all inside a `//`-commented block); `getRandomMomentum`/`updateMomentum`/`updatePositionFromMomentum` are declared in the header (`chomp_optimizer.hpp:212-214`) but have no `ChompOptimizer::` definition anywhere in `chomp_optimizer.cpp` — only `perturbTrajectory` (`:959-990`) has a real, unreachable body | CONFIRMED (TYPE_A) | `chomp_optimizer.cpp:359-361,442` (commented call sites) and `chomp_optimizer.hpp:212-214` vs. `rg -n 'ChompOptimizer::(getRandomMomentum|updateMomentum|updatePositionFromMomentum|perturbTrajectory)' chomp_optimizer.cpp` (only `perturbTrajectory` has a body) — read directly | (uncommitted) |
| `src/planner.rs` module doc, `solve`'s `MotionPlanRequest` field-coverage count (3 of 16 read) | `solve` reads exactly `req.start_state`, `req.group_name`, `req.goal_constraints` and no other `MotionPlanRequest` field | CONFIRMED (TYPE_A) | `rg -n 'req\.' chomp_planner.cpp` (all 10 hits are one of the three named fields) read directly | (uncommitted) |
| `src/planner.rs` module doc, `MotionPlanDetailedResponse`'s 5 fields all written | the hand-written `planning_interface::MotionPlanDetailedResponse` struct has exactly 5 fields (`trajectory`, `description`, `processing_time`, `error_code`, `planner_id`), all assigned somewhere in `solve` | CONFIRMED (TYPE_A) | `moveit_core/planning_interface/include/moveit/planning_interface/planning_response.hpp:75-84` (struct body) read directly | (uncommitted) |
| `src/planner.rs` module doc, "29 lines are pure logging/chrono" | `rg -c 'RCLCPP_\|chrono' chomp_planner.cpp` measurement | CONFIRMED (TYPE_A) | `rg -c 'RCLCPP_|chrono' chomp_planner.cpp` reproduced directly, returns `29` | (uncommitted) |
| `src/planner.rs`, `GoalJointConstraint`'s dual-form rationale | the raw, unresolved joint position is written straight onto `goal_state` (`chomp_planner.cpp:108`, immediately before the `goal_state.satisfiesBounds()` check at `:109`) and a *separately* resolved `kinematic_constraints::JointConstraint` is built from the same raw fields only later, for the final tolerance check | CONFIRMED (TYPE_A) | `chomp_planner.cpp:105-115` (raw-write + bounds check) and `:291-302` (`kinematic_constraints::JointConstraint jc(...)`, `jc.configure(constraint)` in the goal-tolerance loop) read directly | (uncommitted) |
| `src/planner.rs`, `ChompGoal`'s joint-only-by-construction rationale | upstream rejects any goal candidate with a non-empty `position_constraints`/`orientation_constraints`, or an empty `joint_constraints`, with `INVALID_GOAL_CONSTRAINTS` | CONFIRMED (TYPE_A) | `chomp_planner.cpp:97-103` read directly | (uncommitted) |
| `src/planner.rs`, `ChompSolution::planner_id` always `"chomp"` | `res.planner_id = std::string("chomp");` is the first statement in `solve`, unconditional, before the `planning_scene` null check or any other failure branch | CONFIRMED (TYPE_B) | `chomp_planner.cpp:68` read directly | (uncommitted) |
| `src/planner.rs`, `ChompSolution::description` always `"plan"` | `res.description[0] = "plan";` is the only assignment to `res.description`, unconditional on the success path | CONFIRMED (TYPE_B) | `chomp_planner.cpp:273` read directly | (uncommitted) |
| `src/planner.rs`, `ChompRequest::seed_trajectory` is upstream's `res.trajectory[0]` read (not written) only for `"fillTrajectory"` | the `fillTrajectory` branch reads `res.trajectory[0]` (via `res.trajectory.empty()` and `fillInFromTrajectory(*res.trajectory[0])`) and writes nothing to `res.trajectory` anywhere in this branch — `res.trajectory` is written only later, unconditionally, at `:270-271` | CONFIRMED (TYPE_A) | `chomp_planner.cpp:151-164` read directly | (uncommitted) |
| `src/planner.rs`, `build_seed_trajectory`'s upstream span | the start/goal bounds checks, both `robotStateToArray` calls, and the continuous-joint wrap-around fix are exactly upstream `chomp_planner.cpp:76-136` | CONFIRMED (TYPE_B) | `chomp_planner.cpp:76-136` read directly | (uncommitted) |
| `src/planner.rs`, wrap-around fix comment `chomp_planner.cpp:119-136` | the `// fix the goal to move the shortest angular distance for wrap-around joints:` loop spans exactly these lines | CONFIRMED (TYPE_B) | `chomp_planner.cpp:119-136` read directly | (uncommitted) |
| `src/planner.rs`, `solve`'s overall port span `chomp_planner.cpp:63-306` | `ChompPlanner::solve`'s signature starts at `:63`, its closing brace is at `:306` | CONFIRMED (TYPE_B) | `chomp_planner.cpp:63,306` read directly | (uncommitted) |
| `src/planner.rs`, output-trajectory fill-in span `chomp_planner.cpp:255-268` | the `// fill in the entire trajectory` loop building `result` from `trajectory` spans exactly these lines | CONFIRMED (TYPE_B) | `chomp_planner.cpp:255-268` read directly | (uncommitted) |
| `src/planner.rs`, goal-tolerance check span `chomp_planner.cpp:292-301`/`:292-302` (two occurrences, off by one on the closing brace) | the `for` loop calling `jc.configure(constraint)`/`jc.decide(last_state).satisfied` and returning `GOAL_CONSTRAINTS_VIOLATED` spans `:294-302` inclusive of its closing brace | CONFIRMED (TYPE_B) — one-line brace-inclusion fuzziness, not a substantive citation error | `chomp_planner.cpp:291-302` read directly | (uncommitted) |
| `src/planner.rs`, recovery-loop code-comment span `chomp_planner.cpp:177-243` | this broader span (declarations `replan_count`/`replan_flag`/`org_*` at `:177-180` through the `while (true)` loop's closing brace at `:243`) legitimately covers the whole recovery mechanism, unlike the narrower, wrong `:177-181` citation used elsewhere for the `+5` computation specifically (see EXPIRED finding below) | CONFIRMED (TYPE_B) | `chomp_planner.cpp:177-243` read directly | (uncommitted) |
| `src/planner.rs` module doc, "Deviation: `planning_time_limit + 5` is validated before narrowing" citing `chomp_planner.cpp:177-181` for the `setRecoveryParams(...+5...)` call | **Wrong citation.** Lines 177-181 are `int replan_count = 0; bool replan_flag = false; double org_learning_rate = ...; int org_max_iterations = ...;` — plain variable declarations, not the narrowing call. The actual `setRecoveryParams(params_nonconst.learning_rate_ + 0.02, ..., params_nonconst.planning_time_limit_ + 5, ...)` call is at `:200-201`. Same defect family as the two optimizer.rs citation errors above. The underlying behavioral claim (unguarded `double`→`int` narrowing UB) is itself true, verified at the correct line. **Note:** this exact citation, on the exact same claim, is also the citation used by this doc's own pre-existing first table row (marked CONFIRMED, commit `2d54e7d`) — that row is out of this audit's scope to re-litigate/duplicate per the task brief, but its citation is subject to the same correction | EXPIRED (citation line wrong by ~20 lines; substance CONFIRMED) | `chomp_planner.cpp:177-181` (unrelated declarations) vs. `:200-201` (`rg -n 'setRecoveryParams'` — the actual call site) | (uncommitted) |
| `src/planner.rs`, `solve`'s `# Errors` doc, `PlanningFailed`/unset-upstream-error-code claim | **Undercounts and misattributes.** The doc claims upstream "leaves `res.error_code` unset on these two specific paths (`chomp_planner.cpp:151-169`)" for the `PlanningFailed` case, and lists "the optimizer fails to initialize, or `trajectory_initialization_method` is not valid" as those two paths. Two defects: (1) there are actually **three** unset-error-code return paths in `151-169` (`res.trajectory.empty()` at `:153-157`, `fillInFromTrajectory` failing at `:158-163`, and the invalid-method `else` at `:165-169`), not two; (2) the port's own code returns `MoveItErrorCode::Failure` (not `PlanningFailed`) for the invalid-`trajectory_initialization_method` path (`src/planner.rs:404`, the `_ =>` match arm) — `PlanningFailed` is returned only for optimizer-init failure (`:444`), which upstream *does* set explicitly (`res.error_code.val = PLANNING_FAILED;` at `chomp_planner.cpp:211`, outside the cited `151-169` range entirely, so it isn't even one of upstream's "unset" paths) | EXPIRED (miscount: 2 claimed vs. 3 actual; misattribution: invalid-method path returns `Failure` in this port's own code, not `PlanningFailed` as the doc bullet implies) | `chomp_planner.cpp:151-169` (three unset-return branches) and `:208-213` (`PLANNING_FAILED` *is* set here, outside the cited range) read directly; `src/planner.rs:392-404` (match arms) and `:444` cross-checked against the doc at `:363-373` | (uncommitted) |
| `src/planner.rs` test, `solve` hardcodes a 3.0s/0.03s trajectory (101 points) | `ChompTrajectory trajectory(planning_scene->getRobotModel(), 3.0, .03, req.group_name);` — not derived from any request field | CONFIRMED (TYPE_B) | `chomp_planner.cpp:87` read directly | (uncommitted) |
| `src/planner.rs` test, `shortestAngularDistance(3.0, -3.0) == 2*PI - 6.0 == 0.283185307179586...` | hand-computed against upstream's `normalizeAnglePositive`/`normalizeAngle`/`shortestAngularDistance` formulas: `shortestAngularDistance(3.0, -3.0)` evaluates to `0.283185307179586`, matching `(2*PI - 6.0)`, giving an adjusted goal of `3.283185307179586` | CONFIRMED (TYPE_B) | `chomp_utils.hpp:77-85` (formula) read directly; hand-verified by substitution | (uncommitted) |
| `tests/chomp_quad_cost_inverse_parity.rs` module doc, "the op links the real upstream `ChompCost` rather than a transcription of its constructor" | `oracle.cpp`'s `chompQuadCostInverse` op `#include`s the real `<chomp_motion_planner/chomp_cost.hpp>`/`<chomp_motion_planner/chomp_trajectory.hpp>` and directly constructs `chomp::ChompTrajectory`/`chomp::ChompCost`, calling `cost.getQuadraticCostInverse()` — not a reimplementation | CONFIRMED (TYPE_B) | `tools/moveit-oracle/src/oracle.cpp:129-130` (includes) and `:5290-5301` (`chompQuadCostInverse` body) read directly | (uncommitted) |
| `tests/chomp_quad_cost_inverse_parity.rs` module doc, `Eigen::MatrixXd::inverse()` is always `PartialPivLU` | same fact already recorded under `src/cost.rs` module doc's decomposition-family note above — not re-verified as a separate claim, cross-referenced instead | CONFIRMED (TYPE_A) — duplicate of the `src/cost.rs` row above | (see `src/cost.rs` decomposition-family row) | (uncommitted) |
| `tests/chomp_quad_cost_inverse_parity.rs` module doc, "Tolerance" section's measured per-case maxima (`num_points` 13→`1.78e-15` … 20→`2.68e-11`) and "Ground truth was re-run directly against the live oracle when this test was written" | Both are claims about this test's own authoring-time process/instrumentation (a removed `eprintln!`, a one-time oracle re-run) — not independently reproducible without reinstating that instrumentation, which would mean modifying test source, out of this audit's scope. Re-running the test as committed today (`cargo test -p moveit-planners-chomp --test chomp_quad_cost_inverse_parity`) passes with the stated `TOL = 1e-7`, consistent with but not proof of the exact historical per-case figures | UNVERIFIABLE (historical instrumentation removed; current pass is consistent-but-not-confirming evidence) | `tests/chomp_quad_cost_inverse_parity.rs:51-74` (claim text); test re-run directly: `1 passed; 0 failed` | (uncommitted) |

## Background-agent audit — see `moveit-trajectory.md`

The Item-4 background agent's aggregate "3 TYPE_B / 7 TYPE_A out of 60
candidates" count spanned this crate too. Same disposition as recorded
there: **UNVERIFIABLE(itemized list lost to context compaction)** —
none of those specific claims are re-derivable from this session's
surviving context, so none are recorded here as confirmed or expired.
