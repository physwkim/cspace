# moveit-ros type-b claim audit

Every doc-comment in `ros/moveit-ros/src` asserting something about
upstream (moveit2 @ `e017c91ee12984393a28ba246075c65f69cde3bf`,
checked out at `$HOME/work/moveit2`), verified by opening the cited
upstream range directly — not by reasoning from the port. §175 format
(PORTING-PLAN.md `b76e4d4`): one row per item, appended as found, not
batched. This file is the full flush of the round-8 audit that had
been sitting only in conversation context.

`verdict` uses the prescribed three values (CONFIRMED / EXPIRED /
UNVERIFIABLE(reason)); rows where the port's own citation line is
wrong but the claimed substance is independently confirmed true are
still `CONFIRMED`, with the discrepancy spelled out in the row so it
isn't hidden by the verdict — `evidence` always holds the real
upstream location, not the port's (possibly wrong) cited one.

| where | claim | verdict | evidence | commit |
|---|---|---|---|---|
| `scene/collision_object.rs:19-21` | `processCollisionObjectMsg` signature `(&mut PlanningScene, &CollisionObject) -> bool` (the port's own doc literally reads `&CollisionObject)` there) | CONFIRMED | `planning_scene.cpp` processCollisionObjectMsg definition | |
| `scene/collision_object.rs:39-41` | `OCTOMAP_NS` constant matches upstream's | CONFIRMED | `planning_scene.hpp:113` | |
| `scene/collision_object.rs:39-41,294-295` | rejected before dispatch in `processCollisionObjectMsg` (cites `:1758`); the doc comment there names the constant `PlanningScene::OCTOMAP_NS` | CONFIRMED (citation-line wrong: cites `:1758`, actual `:1774`) | `planning_scene.cpp:1774` (`grep -n "^bool PlanningScene::processCollisionObjectMsg"`) | |
| `scene/collision_object.rs:50-52` | ADD=0/REMOVE=1/APPEND=2/MOVE=3, referenced via the generated constants rather than hardcoded so a `third_party/moveit_msgs` repin is followed automatically | CONFIRMED | `third_party/moveit_msgs/msg/CollisionObject.msg` | |
| `scene/collision_object.rs:96-102` (drift: row previously cited `:85-89`, which is now `TryFrom<u8>`'s closing braces, not `is_empty_pose` -- re-derived by locating the function the claim actually describes) | `isEmpty(Pose)` literal `==0.0`/`==1.0` field comparisons, e.g. `pose.orientation.w == 1.0` | CONFIRMED | `message_checks.cpp:77-79` | |
| `scene/collision_object.rs:108-109` (drift: row previously cited `:102-104`, which is now `is_empty_pose`'s closing brace -- `parallel_shapes`'s doc comment naming the lambda moved to 108-109) | `treat_shape_vectors` lambda (cites `planning_scene.cpp:1852`) | CONFIRMED (citation-line wrong: cites `:1852`, actual `:1855`) | `planning_scene.cpp:1855` | |
| `scene/collision_object.rs:113-115` (drift: row previously cited `:108-111`, which is now the `treat_shape_vectors` naming and the "# The asymmetric length rule" header, not the rule's own text -- re-derived to the paragraph that actually states it) | upfront length checks reject poses>shapes (`items.len() < poses.len()` is **rejected**), one RCLCPP_ERROR per array | CONFIRMED | `planning_scene.cpp:1805-1818` | |
| `scene/collision_object.rs:150-152` (`shapes_and_poses_from_collision_object`) (drift: row previously cited `:145-146`, which is now `parallel_shapes`'s push loop, not this doc comment; widened to a range to anchor on the containing fn) | `shapesAndPosesFromCollisionObjectMessage` at `:1800` | CONFIRMED | `planning_scene.cpp:1800` | |
| `scene/collision_object.rs:156-157` (drift: row previously cited `:151-152`, two lines above where the `header_to_pose_transform` phrase this row is about actually sits) | "matching upstream's own out-param name `header_to_pose_transform`" | EXPIRED (never true — fabricated) | `planning_scene.cpp:1801`: actual formal parameter name is `object_pose`; `header_to_pose_transform` is only a caller-local variable at the `processCollisionObjectAdd` call site | |
| `scene/collision_object.rs:157-159` (`shapes_and_poses_from_collision_object`) | single-shape pose-swap mechanism, headed "# The single-shape pose swap" in the doc comment (which also names `header_to_pose_transform.` two lines above) | CONFIRMED | `planning_scene.cpp:1823-1852` (declaration `:1825`, append lambda `:1837`) | |
| `scene/collision_object.rs:208-214` | `subframe_names[]`/`subframe_poses[]` exact-length-only; upstream has no length check at all on either the world-object or attached path | CONFIRMED | `planning_scene.cpp` `processCollisionObjectAdd:1921/1924`, `processAttachedCollisionObjectMsg:1612/1615` — both exact | |
| `scene/collision_object.rs:318-320` (`apply_add`) (drift: row previously cited `:313-315`, which is now the `Remove`/`Move` match arms in `apply_collision_object`, not the ADD/APPEND handling this row describes -- re-derived to `apply_add`'s own doc comment) | `processCollisionObjectAdd` handles ADD/APPEND, differing only in whether existing object is removed first | CONFIRMED | `planning_scene.cpp:1887` | |
| `scene/collision_object.rs:361-370` (`apply_add`) (drift: row previously cited `:356-358`, which is now the `subframes_from_parallel_arrays` call, not the `knowsFrameTransform` check this row describes) | `knowsFrameTransform` checked before world touched | CONFIRMED | `planning_scene.cpp:1889` | |
| `scene/collision_object.rs:408-415` (`apply_add`) (drift: row previously cited `:396-402`, which is now the "creating_fresh" `move_object` comment, not `setSubframesOfObject`) | `setSubframesOfObject` called unconditionally even with empty map | CONFIRMED | `planning_scene.cpp:1927` | |
| `scene/collision_object.rs:420-424` (`apply_remove`) (drift: row previously cited `:408-409`, which is now the `setSubframesOfObject` comment belonging to the row above -- re-derived to `apply_remove`'s own doc comment) | `processCollisionObjectRemove` | CONFIRMED | `planning_scene.cpp:1931` | |
| `scene/collision_object.rs:439-452` (`apply_move`) (drift: row previously cited `:427-432`, which is now inside `apply_remove`, not `apply_move` -- re-derived to `apply_move`'s own doc comment) | `processCollisionObjectMove`, pose always applied unconditionally even if shape-repose fails later | CONFIRMED | `planning_scene.cpp:1953` | |
| `scene/collision_object.rs:509-512` (`apply_move`) (drift: row previously cited `:489-491`, which is now the empty-`shape_poses_msgs` early return in `apply_move`, not the `poseMsgToEigen` ordering comment) | `poseMsgToEigen` parses all poses before `moveShapesInObject` repose call (cites `:1985-1998`) | CONFIRMED (citation range short: cited end `:1998`, conversion loop actually ends `:2002`, call is at `:2004`) | `planning_scene.cpp:1987-2002` (loop), `:2004` (`moveShapesInObject` call) | |
| `scene/planning_scene.rs:75-80` (`is_diff`) (drift, re-derived a second time: the prior `:43-48` was itself wrong, not just stale — it landed in the module's `is_diff`-flag doc block, which discusses the same subject but does not contain the fabricated class name at all; the literal string sits in `is_diff`'s own function doc comment) | `GeometricShapesPlanningSceneMsgConversions` cited as evidence core has no "am I a diff" query | EXPIRED (never true — fabricated class name) | `grep -rni GeometricShapesPlanningSceneMsgConversions` over full moveit2 tree: 0 hits. Underlying conclusion separately CONFIRMED via `planning_scene.hpp` (no `isDiff()`-style method; `parent_` field at `:970`) | |
| `scene/planning_scene.rs:85-92` (`robot_model_name_matches`) (drift, re-derived a second time: the prior `:53-59` was itself wrong — it landed in the module's "Fields with no core representation" doc block, unrelated to `robot_model_name_matches`, which lives 30+ lines further down) | `robot_model_name` mismatch is `RCLCPP_WARN` not hard error, empty name skips check (cites `:1370`) | CONFIRMED (citation-line wrong: `:1370` is the preceding `RCLCPP_DEBUG`, actual check `:1373`) | `planning_scene.cpp:1373` | |
| `scene/planning_scene.rs:507-515` (`apply_planning_scene_world`) (drift, re-derived a second time: the prior `:62-70` was itself wrong — it landed in the module's `use` block near the top of the file; `apply_planning_scene_world`'s own doc comment, which states the "ANDs across all... keeps going" sentence verbatim, sits ~390 lines further down) | `processPlanningSceneWorldMsg`'s `result &=` ANDs across all collision objects, keeps going on failure | CONFIRMED | `planning_scene.cpp:1396` | |
| `scene/planning_scene.rs:526-530` (`apply_octomap`) (drift: this row's line numbers have now been re-derived twice, most recently when `p11-scenetopic` grew this file from 522 to 1367 lines; the range must cover both the `processOctomapMsg` sentence and the empty-`octomap.data` early-return sentence, not stop at the first) | `processOctomapMsg(OctomapWithPose&)` early-returns on empty octomap.data | CONFIRMED | `planning_scene.cpp:1478` (signature), `:1483` (early return) | |
| `geometry.rs:16-19` | tf2_eigen treats both `Point` and `Vector3` as `Eigen::Vector3d` | CONFIRMED | `/opt/ros/rolling/include/tf2_eigen/tf2_eigen/tf2_eigen.hpp:204` (`fromMsg(Point&, Vector3d&)`), `:231` (`fromMsg(Vector3&, Vector3d&)`); `planning_scene.cpp` includes `tf2_eigen/tf2_eigen.hpp` | |
| `geometry.rs:101-106` (drift: row previously cited `:96-99`, upstream of the doc comment's own round-14/15 rewrite growing it; the "normalize unconditionally, never fail" quote and the `.normalize()` call site both sit further down) | quaternion unconditionally normalized, no zero/NaN guard, per `utilities::poseMsgToEigen`'s own "normalize unconditionally, never fail" rule | CONFIRMED (method-name imprecision: claims `.normalized()`, actual `.normalize()`) | `tf2_eigen.hpp:336-340` (`fromMsg(Quaternion&, Quaterniond&)`), `:493-503` (embedded Pose conversion) | |
| `constraints/context.rs:20-22` | non-fixed frame resolves mobile, re-decided against current state every `decide()` call | CONFIRMED | `kinematic_constraint.cpp:382-391` (`isFixedFrame` gate sets `mobile_frame_`), `:528-532` (`decide()` re-transform when `mobile_frame_`) | |
| `constraints/visibility.rs:36-37` (defers to `crates/moveit-constraints/src/visibility.rs`) | `axis_column()` matches upstream `col(2 - sensor_view_direction_)`, with SENSOR_Z=0/SENSOR_Y=1/SENSOR_X=2 | CONFIRMED | `kinematic_constraint.cpp:1058` (`col(2 - sensor_view_direction_)`), `:1077-1078` (upstream's own comment: "necessary to do subtraction as SENSOR_Z is 0 and SENSOR_X is 2") | |
| `scene/attached.rs:19-21` | `AttachedBody` (`moveit_scene::AttachedBody`) has no `weight` field, port drops it | CONFIRMED | `moveit_core/robot_state/include/moveit/robot_state/attached_body.hpp`: 0 hits for a weight field | |
| `scene/attached.rs:19-24` | `detach_posture` "never reads back" inside `planning_scene.cpp`'s own processing (this port's doc comment names the receiving type as `moveit_scene::AttachedBody`) | EXPIRED (partially false as literally worded) | `planning_scene.cpp:1675`: `object.detach_posture.joint_names.empty() ? ab->getDetachPosture() : object.detach_posture;` — it IS read back on APPEND to carry the old value forward when the message doesn't supply a new one. The narrower claim "no invariant branches on its content" still holds (nothing decides behavior based on the trajectory's contents), but "never reads back" is false as stated | |
| `scene/attached.rs:65-69` | MOVE not implemented, exact `RCLCPP_ERROR` quote | CONFIRMED | `planning_scene.cpp:1764`: `RCLCPP_ERROR(getLogger(), "Move for attached objects not yet implemented");` — exact string match | |
| `scene/attached.rs:74-77` | ADD/APPEND shared-path gate condition, this port's doc comment quoting it as `operation == ADD \|\| !hasAttachedBody(id)` (cites `:1567-1622`) | CONFIRMED (citation range wrong: cited range is an earlier `if (link_model)` block, actual gate `object.operation == ADD \|\| !hasAttachedBody(id)` is elsewhere) | `planning_scene.cpp:1648-1649` | |
| `scene/attached.rs:96-98` | world-object promotion gate, guarded in this port by `operation == ADD` per its own comment (cites `:1576`, a comment line) | CONFIRMED (citation-line wrong: `:1576` is a comment, actual condition starts `:1579`) | `planning_scene.cpp:1579-1580` | |
| `scene/attached.rs:148-152` | "APPEND onto an existing attached body: merge" -- this port's own comment names the fresh-attach path as `attach_new` (cites `:1602-1622`) | CONFIRMED (citation range wrong — points at a different code block entirely: `:1602-1622` is STEP-1's message-contents-fill branch, shared by ADD and APPEND when the object isn't already in the world; the actual APPEND-merge-with-old-attached-body logic is a separate STEP-3 `else` block) | `planning_scene.cpp:1663-1686` (`else // APPEND: augment to existing attached object`) | |
| `scene/attached.rs:161-165` | duplicate subframe names: message's value wins over old attached body's, via `std::map::insert` keep-first semantics (cites `:1611`/`:1612` as the insert call) | CONFIRMED (citation-mechanism wrong: `:1611`-`:1615` is `operator[]` assignment filling the map from the message, not an `insert()` call; the actual `std::map::insert(first,last)` call carrying keep-first semantics — and thus the mechanism that makes the message win — is uncited) | `planning_scene.cpp:1611-1617` (message fill via `subframe_poses[name] = subframe_pose;`), `:1673` (`subframe_poses.insert(ab->getSubframes().begin(), ab->getSubframes().end());` — the real insert call) | |
| `scene/attached.rs:256-259` (drift: row previously cited `:247-250,618-621`; `:247-250` is now `apply_attach`'s closing return statement, not this return-expression text -- re-derived to `apply_detach`'s own "Two asymmetries" doc comment, which literally has `!attached_bodies.empty()`) | return expression `!attached_bodies.empty() \|\| object.object.id.empty()` (cites `:1665`) | CONFIRMED (citation-line wrong: cites `:1665`, actual `:1759`) | `planning_scene.cpp:1759` | |
| `scene/attached.rs:267-269` (`apply_detach`) (drift: row previously cited `:256-259`, which is now the return-expression asymmetry text belonging to the row above, not the `link_name.empty() ? nullptr` landmine paragraph) | `link_name.empty() ? nullptr : ...` filter / match-everything branches (cites `:1633` and `:1636`) | CONFIRMED (citation-lines wrong: cites `:1633`/`:1636`, actual `:1701` and `:1704/1708`) | `planning_scene.cpp:1701` (nullptr ternary), `:1704-1708` (filter vs match-all branches) | |
| `constraints/joint.rs:44-46` | `joint_variable_name()` is the exact wire-form string upstream expects, per `moveit_constraints::joint`'s own doc | DEFERRED — second-hand citation, not independently opened against moveit2 this round; verification owned by `crates/moveit-constraints` | (none opened this round) | |
| `constraints/set.rs:7-14` | `KinematicConstraintSet` has no `name` field, per `moveit-constraints`'s own doc (re-checked their round 5) | DEFERRED — second-hand citation, not independently opened against moveit2 this round; verification owned by `crates/moveit-constraints` | (none opened this round) | |
| `planning.rs:402-410` (the `planning_time` paragraph of `TryFrom<PlanningResponseMsg>`'s doc comment; an earlier revision of this row pointed at the sibling `TryFrom<PlanningRequestMsg>` impl's `reference_trajectories` rejection instead, an unrelated impl) | `planning_time` unported, per `crates/moveit-planning/src/response.rs:39-68`'s own conclusion | DEFERRED — second-hand citation; one of the sibling crate's own cited sites spot-checked | `ompl_interface/src/model_based_planning_context.cpp:799` (`res.planning_time = ptime;`) — spot-check CONFIRMED; `planning_pipeline.cpp` confirmed 0 hits for `planning_time` as claimed | |
| (crate-wide, §172 boundary, not this round's new work) | `rclcpp::Duration::from_seconds` — upstream narrowing behavior for negative/non-finite/>i32::MAX inputs | UNVERIFIABLE(source absent both on host and in `moveit-rs/ros-dev:latest` docker image — only `duration.hpp`'s declaration at `:135` exists anywhere on this machine, no `.cpp` implementation) | `duration.hpp:135` (declaration only) | (rejected at the boundary instead, see PORTING-PLAN.md §172.4, `929a25c`) |

Round 9, task 3: `80c21bf` (octomap wiring) added new upstream claims once the decoder existed to wire against. Verified against the exact upstream lines below, not inferred from the round-7 plan.

| where | claim | verdict | evidence | commit |
|---|---|---|---|---|
| `scene/planning_scene.rs:540-543` (drift, re-derived a second time: the prior `:94-98` was itself wrong — it landed near the top-of-file `use` block; `apply_octomap`'s own doc comment states this sentence verbatim ~390 lines further down) | `msg.binary` selects the decode entry point (`read_binary_data`'s 2-bit-per-child form vs. `read_data`'s per-node raw `f32`); the two wire formats are structurally different, neither is a fallback for the other | CONFIRMED | `planning_scene.cpp:1417-1435` (`createOctomap`: `if (map.binary) octomap_msgs::readTree(om.get(), map); else ... om->readData(datastream);`) | |
| `scene/planning_scene.rs:593` (drift, re-derived a second time: the prior `:148` was itself wrong — it landed inside `use_planning_scene_msg`'s closing brace, unrelated; the actual `OcTreeShape::from_tree(Arc::new(tree))` call is inside `apply_octomap`, ~390 lines further down) | `OcTreeShape::from_tree(Arc<OcTree>)` matches upstream's octree-to-shape construction | CONFIRMED | `third_party/geometric_shapes/include/geometric_shapes/shapes.h:380` (`OcTree(const std::shared_ptr<const octomap::OcTree>& t)`), call site `planning_scene.cpp:1498` (`std::make_shared<const shapes::OcTree>(om)`) | |
| `scene/planning_scene.rs:570,572-574` (drift, re-derived a second time: the prior `:70,125-129` was itself wrong — neither line is inside `apply_octomap`; the actual `scene.remove_object(OCTOMAP_NS)` call and the empty-data check that follows it are ~390 lines further down) | octomap always replaces any previous one — `world_->removeObject(OCTOMAP_NS)` unconditional, before the payload is even checked | CONFIRMED | `planning_scene.cpp:1481` (`removeObject` first), `:1483` (empty-data check comes after) | |
| `scene/planning_scene.rs:550-557` (drift, re-derived a second time: the prior `:104-111` was itself wrong — it landed in the `PlanningSceneUpdate` enum/dispatch code, unrelated; `apply_octomap`'s own doc comment states this composition rule ~390 lines further down) | `map.origin` is header-relative; upstream composes `t = getFrameTransform(map.header.frame_id); p = t * p` before insert | CONFIRMED — was a real gap in this port (`apply_octomap` used `map.origin` directly, no composition), found while opening these exact lines to verify the reserved-id-replacement claim above, not by upstream disagreeing with the port. Fixed same round, commit `0e3f706` | `planning_scene.cpp:1494` (`getFrameTransform` call), `:1497` (`p = t * p`) | `0e3f706` |
| `scene/planning_scene.rs:559-565` (deviation, not a defect) (drift, re-derived a second time: the prior `:104-111` was itself wrong — same misplaced citation as the row above; `apply_octomap`'s doc comment states the no-`knowsFrameTransform`-guard asymmetry a few lines after the composition rule) | the `OctomapWithPose` overload calls `getFrameTransform(map.header.frame_id)` unconditionally — unlike its non-`WithPose` sibling overload, which guards with `if (!map.header.frame_id.empty())` and uses `Identity()` directly when empty | CONFIRMED (as upstream's own asymmetry between the two overloads, not a port defect) | `planning_scene.cpp:1453` (sibling's empty-check guard), `:1494` (`WithPose` overload's unconditional call) — this port's own `frame_transform` already errors rather than upstream's silent-identity-on-unresolved fallback (a pre-existing, deliberate D6 deviation, `scene.rs:1289-1361`), so an empty `header.frame_id` on a real `OctomapWithPose` message now hard-errors here where upstream would silently resolve to identity via `getFrameTransform`'s own internal fallback | |
