# Claim audit — `moveit-smoothing`

Per `PORTING-PLAN.md` §175: one row per claim about upstream behaviour
found in this crate, verified against the actual upstream source (not
inferred from the port), appended as found rather than batched at
report time.

| where | claim | verdict | evidence | commit |
|---|---|---|---|---|
| `crates/moveit-smoothing/src/lib.rs:16-27` | TYPE_A: `ButterworthFilterPlugin` excluded (D1) — reads `butterworth_filter_coeff` from a `ParamListener`; `initialize` takes `rclcpp::Node::SharedPtr` | CONFIRMED | `butterworth_filter.hpp:101` (`initialize(rclcpp::Node::SharedPtr node, ...)`); `butterworth_filter.cpp:101-115` (`ParamListener param_listener(node_); ... butterworth_filter_coeff`) | (uncommitted) |
| `crates/moveit-smoothing/src/lib.rs:28-37` | TYPE_A: `smoothing_base_class.cpp` excluded (D1+D4) — `initialize` takes `rclcpp::Node::SharedPtr` in the trait itself; class is `pluginlib`-loadable (`PLUGINLIB_EXPORT_CLASS`); `.cpp` is a default ctor/dtor with no other content | CONFIRMED | `smoothing_base_class.hpp:71` (`virtual bool initialize(rclcpp::Node::SharedPtr node, ...) = 0;`); `smoothing_base_class.cpp:43-44` (`= default` ctor/dtor, nothing else in file); `butterworth_filter.cpp:155`, `ruckig_filter.cpp:219`, `acceleration_filter.cpp:410` each carry `PLUGINLIB_EXPORT_CLASS` | (uncommitted) |
| `crates/moveit-smoothing/src/lib.rs:38-46` | TYPE_A: `printRuckigState` (private) not ported — a diagnostic with no effect on computed output | CONFIRMED | `ruckig_filter.cpp:210-215` — only calls `RCLCPP_INFO_STREAM`, never mutates `ruckig_input_`/`ruckig_output_`/`ruckig_` | (uncommitted) |
| `crates/moveit-smoothing/src/lib.rs:47-58` | TYPE_A: `jointLimitAccelerationScalingFactor` (free function) ported as a private helper of the same name's-worth of behavior | CONFIRMED | `acceleration_filter.cpp:258-283` — free function (not a class member) in `online_signal_smoothing` namespace, matching `acceleration_filter.rs:181-197`'s private `joint_limit_acceleration_scaling_factor` | (uncommitted) |
| `crates/moveit-smoothing/src/lib.rs:71-72` | TYPE_B: the four headers plus "their four `.h` deprecated-forwarding-shim siblings" | CONFIRMED | `online_signal_smoothing/include/moveit/online_signal_smoothing/{acceleration_filter,butterworth_filter,ruckig_filter,smoothing_base_class}.h` all exist and are `#pragma message(".h header is obsolete...")` + `#include ".../<name>.hpp"` forwarding shims, nothing else | (uncommitted) |
| `crates/moveit-smoothing/src/lib.rs:110-115` | TYPE_B: `ButterworthFilter`'s test module includes both upstream `SMOOTHING_PLUGINS` gtest cases (`FilterConverge`/`FilterReset`) | CONFIRMED | `test_butterworth_filter.cpp:45-73` — `TEST(SMOOTHING_PLUGINS, FilterConverge)`/`TEST(SMOOTHING_PLUGINS, FilterReset)`, identical coefficient (`2.0`) and expected values to `butterworth.rs:122-144`'s `upstream_test_filter_converge`/`upstream_test_filter_reset` | (uncommitted) |
| `butterworth.rs:14-24` | TYPE_A: `EPSILON = 1e-9` matches upstream's anonymous-namespace `EPSILON` in `butterworth_filter.cpp` | CONFIRMED | `butterworth_filter.cpp:48-51` — `namespace { constexpr double EPSILON = 1e-9; }` | (uncommitted) |
| `butterworth.rs:37-41` | TYPE_A: upstream's `FILTER_LENGTH` constant plus a `static_assert(FILTER_LENGTH == 2)` guards that `previous_measurements_` has exactly two slots | CONFIRMED | `butterworth_filter.hpp:82` (`static constexpr std::size_t FILTER_LENGTH = 2;`); `butterworth_filter.cpp:60-61` (`static_assert(ButterworthFilter::FILTER_LENGTH == 2, ...)`) | (uncommitted) |
| `butterworth.rs:59-65` | TYPE_B: `Error::Construct` in the same four cases, checked in the same order, that upstream's constructor throws `std::length_error` | CONFIRMED | `butterworth_filter.cpp:63-79` — order is `isinf(feedback_term_)` → `isinf(scale_term_)` → `low_pass_filter_coeff < 1` → `abs(feedback_term_) < EPSILON`, matching `butterworth.rs:70-89` exactly | (uncommitted) |
| `butterworth.rs:194-200` | TYPE_B: a NaN coefficient constructs successfully with NaN-valued internal state upstream (`isinf(NaN)` false, every `<` comparison against NaN false) | CONFIRMED | `butterworth_filter.cpp:63-79`'s four checks (`isinf`/`isinf`/`< 1`/`abs(...) < EPSILON`) are all false for a NaN-derived `feedback_term_`/`scale_term_` per IEEE-754 semantics (`std::isinf(NAN) == false`; all relational comparisons with NaN are false) | (uncommitted) |
| `ruckig_filter.rs:14-28` | TYPE_A: upstream's `initialize()` uses `node` only to construct a `ParamListener` and read `planning_group_name`/`update_period`; nothing else in `doSmoothing`/`reset`/`getVelAccelJerkBounds` touches `node` | CONFIRMED | `ruckig_filter.cpp:52-59` (only use of `node`); `doSmoothing` (`:82-137`), `reset` (`:139-150`), `getVelAccelJerkBounds` (`:152-208`) never reference `node_`/`node` | (uncommitted) |
| `ruckig_filter.rs:29-40` | TYPE_A: upstream's `initialize()` can fail only via `getVelAccelJerkBounds` returning `false` | CONFIRMED | `ruckig_filter.cpp:52-80` — the only `return false;` in `initialize` is at line 66, gated on `!getVelAccelJerkBounds(...)` | (uncommitted) |
| `ruckig_filter.rs:41-50` | TYPE_A: upstream's `doSmoothing`/`reset` do not validate `positions`/`velocities`/`accelerations` lengths at all | CONFIRMED | `ruckig_filter.cpp:82-150` — no length/size check anywhere in `doSmoothing` or `reset`, unlike `AccelerationLimitedPlugin::doSmoothing` (`acceleration_filter.cpp:312-328`) which does check | (uncommitted) |
| `ruckig_filter.rs:51-54` | TYPE_A: `printRuckigState`/`RCLCPP_ERROR_STREAM`/`RCLCPP_WARN_STREAM` are diagnostics with no effect on computed output | CONFIRMED | `ruckig_filter.cpp:87-89,120-121,158-159,178-179,189-190,201-203,210-215` — all logging-only call sites, none feed back into `ruckig_input_`/`ruckig_output_`/return values | (uncommitted) |
| `ruckig_filter.rs:55-77`, `acceleration_filter.rs:48-83` | TYPE_A: `getVariableBounds(joint->getName())` fails for a multi-DOF joint (no variable is ever named identically to the joint); upstream's own multi-DOF handling elsewhere in `online_signal_smoothing` (`AccelerationLimitedPlugin::initialize`) is a real bug (write index advanced once per joint, not once per variable), not a real algorithm to port | CONFIRMED | `joint_model.cpp:85-91,177-180` (`getLocalVariableIndex` throws `moveit::core::Exception` on lookup miss); `planar_joint_model.cpp:56-61`/`floating_joint_model.cpp:66-75` (variables namespaced `"{name}/{local}"`, never bare joint name — contrast `revolute_joint_model.cpp:59`/`prismatic_joint_model.cpp:48`, which do use the bare name); `acceleration_filter.cpp:189-207` — `ind++` (line 206) sits outside the inner per-variable loop (lines 192-205), so a multi-DOF joint's variables all overwrite the same slot and `ind` under-advances for every subsequent joint; contrast the correctly-placed `++idx` *inside* the inner loop at `acceleration_filter.cpp:264-279`'s `jointLimitAccelerationScalingFactor` in the same file | (uncommitted) |
| `ruckig_filter.rs:78-89` | TYPE_A: the oracle op loads `RuckigFilterPlugin`/`AccelerationLimitedPlugin` via `pluginlib::ClassLoader` plus a never-spun `rclcpp::Node`, since `moveit_ruckig_filter`/`moveit_acceleration_filter` sit under the non-exported `moveit_core_pluginTargets` CMake export set | CONFIRMED | `moveit_core/CMakeLists.txt:129-143` — `moveit_acceleration_filter`/`moveit_butterworth_filter`/`moveit_ruckig_filter` installed under `EXPORT moveit_core_pluginTargets` (line 138), which is never passed to `ament_export_targets` (only `moveit_coreTargets` is, line 143); `tools/moveit-oracle/src/oracle.cpp:4365-4368,4496-4499` load both plugins via `pluginlib::ClassLoader<SmoothingBaseClass>` with a one-off `rclcpp::Node` that is never spun | (uncommitted) |
| `ruckig_filter.rs` do_smoothing doc | TYPE_B: `velocities`/`accelerations` are output-only upstream (never read, only overwritten); `target_velocity` is extrapolated from current velocity/acceleration with no external target-velocity input, matching upstream having none either | CONFIRMED | `ruckig_filter.cpp:82-137` — `velocities`/`accelerations` parameters are only ever assigned (lines 130-133), never read anywhere in the function body; `target_velocity` computed at lines 100-105 from `current_velocity + current_acceleration * update_period` | (uncommitted) |
| `acceleration_filter.rs:32-41` | TYPE_A: upstream's own `doSmoothing`/`reset` signatures keep a third `Eigen::VectorXd& accelerations` parameter that neither reads nor writes it | CONFIRMED | `acceleration_filter.cpp:309-310` (`Eigen::VectorXd& /* unused */` for `accelerations` in `doSmoothing`), `:397-398` (`const Eigen::VectorXd& /* unused */` in `reset`) | (uncommitted) |
| `acceleration_filter.rs:42-47` | TYPE_A: `positions_offset_`/`velocities_offset_` are upstream `AccelerationLimitedPlugin` members read nowhere outside `doSmoothing` | CONFIRMED | `acceleration_filter.hpp:177-178` (members); only written/read inside `doSmoothing` (`acceleration_filter.cpp:350-351`), never in `reset` (`:397-405`) or the destructor (`acceleration_filter.hpp:157-163`) | (uncommitted) |
| `acceleration_filter.rs:84-94` | TYPE_A: upstream names its length-check variable `num_positions` but assigns it from `velocities.size()`, not `positions.size()` | CONFIRMED | `acceleration_filter.cpp:312-313` — `const size_t num_positions = velocities.size(); if (num_positions != num_joints_)` | (uncommitted) |
| `acceleration_filter.rs:95-122` | TYPE_A: upstream's `initialize()` only overrides `warm_starting`/`verbose` (not `eps_abs`/`eps_rel`); `osqp`'s documented default `eps_abs = eps_rel = 1e-3`; fixture case 3 (`panda_joint1`, `M=0.0`, target `3.0`) has `osqp` return `0.0008294991991130152`, an alpha-space error of about `2.76e-4`, well under that default | CONFIRMED | `acceleration_filter.cpp:219-226` (`osqp_set_default_settings(&osqp_settings_);` then only `warm_starting`/`verbose` overridden — no `eps_abs`/`eps_rel`); upstream `osqp/osqp` `include/public/osqp_api_constants.h:159-160` (`OSQP_EPS_ABS (1E-3)` / `OSQP_EPS_REL (1E-3)`, same value in tag `v0.6.3`'s `include/constants.h:61-62`); `acceleration_filter_request.json` case index 2 (`panda_joint1.acceleration_bounds=0.0`, command position `3.0`) and `acceleration_filter_response.json` case index 2 (`panda_joint1: 0.0008294991991130152`) — alpha = 1 − 0.0008294991991130152/3.0 ≈ 0.99972347, error from the exact `alpha=1.0` optimum ≈ 2.765e-4 | (uncommitted) |
| `acceleration_filter.rs:289-299` | TYPE_A: upstream's own `reset` has no length check (unlike `doSmoothing`) | CONFIRMED | `acceleration_filter.cpp:397-405` — `AccelerationLimitedPlugin::reset` has no size/length validation, contrasted with `doSmoothing`'s two checks at `:312-328` | (uncommitted) |
| `acceleration_filter.rs:446-451` (`joint_acceleration_bounds_fails_without_acceleration_limits` doc) | TYPE_A: "not a transcribed upstream test (upstream's own bounds come from a real launch-time `joint_limits.yaml`, never exercised missing)" | EXPIRED | `test_acceleration_filter.cpp:80-104` (`TEST_F(AccelerationFilterTest, FilterInitialize)`), specifically lines 96-98: `// fail because the robot does not have acceleration limits` / `setLimits({});` / `EXPECT_FALSE(filter.initialize(node, robot_model_, PANDA_NUM_JOINTS));` — upstream's own gtest suite directly exercises exactly the "missing acceleration limits" scenario this comment claims is "never exercised". The claim's premise is false, not merely stale; the "not a transcribed upstream test" framing (the Rust test isn't a line-for-line port of `FilterInitialize`) is separately true but does not rescue the "never exercised missing" assertion. | (uncommitted) |
| `ruckig_filter.rs` module doc / `ruckig_filter_parity.rs:58-61` | TYPE_A: `rsruckig` is an independent Rust reimplementation of the same published algorithm, not a binding to upstream's C++ `ruckig` | CONFIRMED | `~/.cargo/registry/src/index.crates.io-*/rsruckig-3.0.0` — pure-Rust crate source, no `build.rs`, no `.c`/`.cpp` files; `Cargo.toml` describes it as "Ruckig motion planning library for Rust" (an independent reimplementation, not an FFI binding to `pantor/ruckig`) | (uncommitted) |
