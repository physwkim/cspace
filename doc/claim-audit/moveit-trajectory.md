# Claim audit — `moveit-trajectory`

Per `PORTING-PLAN.md` §175: one row per claim about upstream behaviour
found in this crate, verified against the actual upstream source (not
inferred from the port), appended as found rather than batched at
report time.

| where | claim | verdict | evidence | commit |
|---|---|---|---|---|
| `src/trajectory.rs`, `Trajectory::integrate_forward` doc | "upstream's own two call sites do [pass `output.trajectory_` for both]" | EXPIRED | `moveit_core/trajectory_processing/src/time_optimal_trajectory_generation.cpp:370` is the *only* call site of `integrateForward` (confirmed via `rg -rn integrateForward moveit_core/trajectory_processing/`: one declaration at `.hpp:166`, one call at `.cpp:370`, one definition at `.cpp:564` — zero other calls) | `705ea76` |
| `src/time_optimal_trajectory_generation.rs`, `totg_compute_time_stamps_silently_collapses_duplicate_waypoints_matching_upstream` doc | for two identical input waypoints, upstream's `totgComputeTimeStamps` degenerates to a 1-waypoint `Ok`/`true` result via the *same* `points.size() == 1` early return firing in both of its internal `computeTimeStamps` calls — not via `new_resample_dt` (`0.0` in this case) ever being read as a divisor | CONFIRMED | `time_optimal_trajectory_generation.cpp:1137-1160` (`totgComputeTimeStamps`, two sequential `computeTimeStamps` calls against the same mutated `trajectory`) and `:1219-1226` (`points.size() == 1` early return, `return true` before `sample_count` is computed) — read directly. Independently reproduced against this crate's own `totg_compute_time_stamps` (temporary `#[test]` probe, output `Ok(())`/`way_point_count == 1`, not committed) before writing the permanent regression test | `92625bb` |
| `src/planner.rs` (`moveit-planners-chomp`, listed under that crate's own file below) | — | — | — | — |
| `tests/totg_parity.rs:6-9` | TYPE_B: `Path`/`Trajectory` core corresponds to `time_optimal_trajectory_generation.hpp` lines 62-192; `TimeOptimalTrajectoryGeneration` adapter starts at header line 193 | CONFIRMED | `hpp:62` `class PathSegment`, `hpp:87` `class Path`, `hpp:124` `class Trajectory` (closes 190), `hpp:193` `class TimeOptimalTrajectoryGeneration : public TimeParameterization` — read directly, boundaries exact | (uncommitted) |
| `tests/totg_parity.rs:22-24` | TYPE_B: zero-length path's NaN duration/velocity/acceleration comes from upstream's own 0/0 divide | CONFIRMED | `time_optimal_trajectory_generation.cpp:405`: `it->time_ = previous->time_ + (it->path_pos_ - previous->path_pos_) / ((it->path_vel_ + previous->path_vel_) / 2.0);` — degenerate zero-length path drives both numerator and denominator to 0 (0/0 = NaN), propagated by later `getPosition`/`getVelocity`/`getAcceleration` divisions — read directly, confirmed myself as well as by the sub-agent | (uncommitted) |
| `tests/totg_parity.rs:28` | TYPE_B: case 5 reuses `upstream_test2`'s multi-segment path | UNVERIFIABLE(the waypoint literals it references live in `trajectory.rs`'s `upstream_test2`, verified separately below; upstream `TEST(time_optimal_trajectory_generation, test2)` itself confirmed to exist at `test_time_optimal_trajectory_generation.cpp:112-158`, 5 waypoints, `max_deviation=100.0`) | — | (uncommitted) |
| `tests/totg_parity.rs:37-41` | TYPE_B: none of the 5 test cases exercise `Path::create`'s `max_deviation<=0.0` error, its 180°-turn error, or `Trajectory::create`'s `time_step<=0.0` error — upstream rejects all three before any numerics run | CONFIRMED | `cpp:203-207` (`waypoints.size()<2`), `cpp:208-212` (`max_deviation<=0.0`), `cpp:236-247` (180°/`cos_angle<=-1.0+angle_tolerance`) all `return std::nullopt` before constructing any `CircularPathSegment`; `cpp:361-365` `Trajectory::create` checks `time_step<=0` and returns `std::nullopt` before constructing `Trajectory output(...)` — read directly | (uncommitted) |
| `tests/totg_parity.rs:45-48` | TYPE_A: algorithm is Kunz & Stilman's, transcribed "instruction-for-instruction" from upstream | CONFIRMED (attribution only); UNVERIFIABLE(the "instruction-for-instruction" transcription-fidelity claim depends on `trajectory.rs`, outside this file) | `hpp:5` `Author: Tobias Kunz`, `hpp:9` `Director: Mike Stilman` — attribution accurate | (uncommitted) |
| `tests/totg_path_parity.rs` (tolerance-measurement doc sections, e.g. max diff `2.27e-13`/`1.05e-15`) | TYPE_B: numeric floor measurements against a captured oracle fixture | UNVERIFIABLE(describes running this crate's own code against a captured JSON fixture, not an upstream-source fact; no upstream file/line is cited to check) | — | (uncommitted) |
| `tests/totg_robot_trajectory_parity.rs:16-19` | TYPE_B: fixture uses upstream `testCustomLimits`'s panda_arm limits, matching upstream's own `setAccelerationLimits` test workaround for a URDF with no acceleration field | CONFIRMED | `test_time_optimal_trajectory_generation.cpp:209` `TEST(..., testCustomLimits)` calls `setAccelerationLimits(robot_model)` at `:216`; `setAccelerationLimits` (`:53-67`) comment: "The URDF used in `loadTestingRobotModel()` does not contain acceleration limits, so add them here." — read directly | (uncommitted) |
| `tests/totg_robot_trajectory_parity.rs:30-35` | TYPE_B: `verifyScalingFactor` clamps out-of-`(0,1]` scaling factors to default `1.0`, logging `"Invalid max_%sscaling_factor %f specified, defaulting to %f instead."` | CONFIRMED | `cpp:1289-1310` `TimeOptimalTrajectoryGeneration::verifyScalingFactor`: `if (requested_scaling_factor > 0.0 && requested_scaling_factor <= 1.0) { scaling_factor = requested_scaling_factor; } else { RCLCPP_WARN(getLogger(), "Invalid max_%sscaling_factor %f specified, defaulting to %f instead.", ...); }`, `DEFAULT_SCALING_FACTOR = 1.0` — read directly myself, format string matches exactly | (uncommitted) |
| `tests/totg_robot_trajectory_parity.rs:36-40` | TYPE_B: a duplicate waypoint (case 2) is dropped by the `min_angle_change` dedup pass | CONFIRMED | `cpp:1186-1216` `doTimeParameterizationCalculations`: `diverse_point` computed via `std::fabs(new_point[j]-points.back()[j]) > min_angle_change_`; non-diverse points dropped (or merged into the final point if last) — read directly | (uncommitted) |
| `tests/totg_robot_trajectory_parity.rs:42-49` and `tests/totg_synthetic_parity.rs:19-31` | TYPE_B: oracle's `hasMixedJointTypesForGroup` re-implements upstream's **private** `TimeOptimalTrajectoryGeneration::hasMixedJointTypes` over the same public `getActiveJointModels()` API; upstream never gates `computeTimeStamps` on it, only warns | CONFIRMED | `hpp:258` `private:`, `hpp:268` declares `hasMixedJointTypes`; `cpp:1273-1288` impl uses `group->getActiveJointModels()`; sole call site `cpp:1176-1180`: `if (hasMixedJointTypes(group)) { RCLCPP_WARN(...) }` with no `return false` — execution continues, confirmed only-warns-no-gate — read directly myself (`sed -n '1170,1290p' src/time_optimal_trajectory_generation.cpp`) plus `rg hasMixedJointTypes` giving exactly one decl + one call + one def | (uncommitted) |
| `tests/totg_robot_trajectory_scaling_only_parity.rs:17-27` | TYPE_A/B: test sets acceleration bounds the same way upstream's `joint_limits.yaml` loaders do — read `variable_bounds_msg`, overwrite only acceleration fields, write back via `set_variable_bounds` | CONFIRMED | `moveit_ros/planning/robot_model_loader/src/robot_model_loader.cpp:129` `joint_model->getVariableBoundsMsg()`, `:178-219` sets `has_acceleration_limits`/`max_acceleration`, `:246` `joint_model->setVariableBounds(joint_limit)`; method names confirmed at `robot_model/include/moveit/robot_model/joint_model.hpp:356,359,362` — read directly | (uncommitted) |
| `tests/totg_robot_trajectory_parity.rs`, `tests/totg_robot_trajectory_scaling_only_parity.rs`, `tests/totg_synthetic_parity.rs` (per-group tolerance-measurement doc sections) | TYPE_B: max-diff floor measurements (`duration_from_previous` `1.39e-17`, `positions` `4.44e-16`, etc.) and bisection methodology | UNVERIFIABLE(measurement of running code against a captured oracle fixture; no upstream source claim to check) | — | (uncommitted) |
| `tests/ruckig_parity.rs:7-9` | TYPE_B: upstream `test_ruckig_traj_smoothing.cpp`'s 4 cases are already ported as direct unit tests, asserting success/duration-bounds only "which upstream's own test does too" | CONFIRMED | `test_ruckig_traj_smoothing.cpp` has exactly 4 `TEST_F(RuckigTests, ...)`: `basic_trajectory` (:62), `basic_trajectory_with_custom_limits` (:82), `trajectory_duration` (:108), `single_waypoint` (:142) — all use `EXPECT_TRUE`/duration-bound assertions only, no position/velocity numeric checks — read directly | (uncommitted) |
| `tests/ruckig_parity.rs:11-13` | TYPE_B: cross-checks against the real `trajectory_processing::RuckigSmoothing::applySmoothing` | CONFIRMED | `ruckig_traj_smoothing.hpp:45` `class RuckigSmoothing`, three `static bool applySmoothing(...)` overloads at `:57,74,90`, in `namespace trajectory_processing` — read directly | (uncommitted) |
| `tests/ruckig_parity.rs:17-22` | TYPE_B: fixture's 7 cases map to `basic_trajectory`/`trajectory_duration`/`basic_trajectory_with_custom_limits`/`single_waypoint`+empty (the `num_waypoints < 2` no-op path) | CONFIRMED | All three named upstream tests exist verbatim at the lines above; `num_waypoints < 2` no-op path confirmed at `ruckig_traj_smoothing.cpp:74-75` and `:108-109` (both `applySmoothing` overloads) — read directly | (uncommitted) |
| `tests/ruckig_parity.rs:33-37` | TYPE_B: `run_ruckig` never rewrites a waypoint's position — only `duration_from_previous`, and only on the duration-extension path does it also touch velocity/acceleration | CONFIRMED | Upstream `RuckigSmoothing::runRuckig` (`ruckig_traj_smoothing.cpp:247-336`): only mutation is `trajectory.setWayPointDurationFromPrevious(...)` at `:299`; `extendTrajectoryDuration` (`:338-361`, retry/overshoot path only) calls `setVariableVelocity`/`setVariableAcceleration` at `:354,359` but never `setVariablePosition` anywhere in either function — read directly | (uncommitted) |
| `tests/ruckig_parity.rs:24-50` | TYPE_B: "Exactness, not tolerance" — zero diffs measured across all 7 cases' waypoints, `duration_from_previous` bit-identical | UNVERIFIABLE(requires re-running the port against the captured oracle fixture; the structural `run_ruckig` reasoning above is independently confirmed, but the "zero diff on this fixture" measurement outcome itself cannot be re-derived from source reading) | — | (uncommitted) |
| `src/ruckig_smoothing.rs:13-15` | TYPE_A: upstream `RuckigSmoothing` is a static-method-only class; port uses free functions instead | CONFIRMED | `ruckig_traj_smoothing.hpp:45-165` — `class RuckigSmoothing { public: static bool applySmoothing(...) x3; }`, no instance state, all `static` | (uncommitted) |
| `src/ruckig_smoothing.rs:19-25` | TYPE_A: the `JointLimits`-message overload is a thin wrapper unpacking into the same vel/accel/jerk maps `apply_smoothing_with_limits` takes | CONFIRMED | `ruckig_traj_smoothing.cpp:154-180` — builds three `unordered_map<string,double>` then calls the map-overload `applySmoothing` at 178-179 | (uncommitted) |
| `src/ruckig_smoothing.rs:34-42` | TYPE_B: upstream defaults vel/accel scaling=1.0, `mitigate_overshoot=false`, `overshoot_threshold=0.01` | CONFIRMED | `ruckig_traj_smoothing.hpp:57-60,74-80` — all three overloads default exactly these values | (uncommitted) |
| `src/ruckig_smoothing.rs:43-47` | TYPE_A: `getRobotModelBounds` has no path returning `false`; port makes it infallible | CONFIRMED | `ruckig_traj_smoothing.cpp:193-245` — only reaches `return true;` at line 244; every branch just assigns and warns | (uncommitted) |
| `src/ruckig_smoothing.rs:48-56` | TYPE_A: `RCLCPP_WARN_STREAM_ONCE`/`RCLCPP_ERROR*` not ported; the two `Error` cases match upstream's two `RCLCPP_ERROR`+`return false` sites | CONFIRMED | `ruckig_traj_smoothing.cpp:69-72`+`182-191` (missing group) and `:323-333` (Ruckig failing after max duration extension); the three `WARN_STREAM_ONCE` calls are at `getRobotModelBounds` lines 212,224,237 | (uncommitted) |
| `src/ruckig_smoothing.rs:57-67` | TYPE_A/B: `extendTrajectoryDuration`'s header doc (`num_waypoints`, "every trajectory segment") mismatches the `.cpp` definition (`waypoint_idx`, single segment) | CONFIRMED | `ruckig_traj_smoothing.hpp:147-159` declares `size_t num_waypoints`, docs "every trajectory segment"; `.cpp:338-361` defines `size_t waypoint_idx`, touches only waypoint `idx+1` — genuine upstream header/impl doc mismatch | (uncommitted) |
| `src/ruckig_smoothing.rs:68-78` | TYPE_A: `getVariableIndexList()` and `JointModelGroup::variable_names()` walk the same per-joint order | CONFIRMED | `joint_model_group.cpp:135-168` — `variable_index_list_` and `variable_names_` appended in the same inner loop over `name_order` | (uncommitted) |
| `src/ruckig_smoothing.rs:79-90` (first half) | TYPE_B: `IgnoreErrorHandler` matches upstream's undecorated `ruckig::Ruckig<ruckig::DynamicDOFs>`, which "never throws" | UNVERIFIABLE(the C++ `ruckig` library itself, upstream's dependency, is not vendored anywhere under `/home/stevek/work/moveit2/` or elsewhere on this machine — only the unrelated Rust `rsruckig` crate is present; per search-then-ask rule, not fabricated) | — | (uncommitted) |
| `src/ruckig_smoothing.rs:79-90` (second half) | TYPE_B: `run_ruckig`, like upstream, drives the retry loop off the `RuckigResult`/`Result` value rather than exceptions | CONFIRMED | `ruckig_traj_smoothing.cpp:267-333` — loop driven entirely by comparing `ruckig_result` against `Result::Working`/`Result::Finished`, no try/catch | (uncommitted) |
| `src/ruckig_smoothing.rs:91-94` | TYPE_B: `checkOvershoot`/`check_overshoot` only needs resampled position; upstream requests velocity/acceleration but never reads either back | CONFIRMED | `ruckig_traj_smoothing.cpp:433-436` — `new_velocity`/`new_acceleration` populated by `at_time` but never referenced afterward | (uncommitted) |
| `src/ruckig_smoothing.rs:162-163` | TYPE_B: a trajectory with <2 waypoints is returned unmodified — "nothing to smooth" | CONFIRMED | `ruckig_traj_smoothing.cpp:74-80,108-114` — `RCLCPP_WARN` + `return true;`, no mutation | (uncommitted) |
| `src/ruckig_smoothing.rs:196-201` | TYPE_B: custom vel/accel limits are still scaled by the caller's factors; jerk limits never scaled | CONFIRMED | `ruckig_traj_smoothing.cpp:135,141` (scaled) vs `:147` (`ruckig_input.max_jerk.at(j) = it->second;`, unscaled) | (uncommitted) |
| `src/ruckig_smoothing.rs:297,321-322,326-328,342,345` | TYPE_B: five inline comments transcribed verbatim/near-verbatim from upstream (angle-wrap warning; overshoot-check-period stepping; Working-vs-Finished distinction; duration-extension trigger; trajectory reset) | CONFIRMED | `ruckig_traj_smoothing.cpp:256,283-284,291-293,304,308` — each matches the cited port comment | (uncommitted) |
| `src/ruckig_smoothing.rs:584-586` (test doc) | TYPE_B: upstream scales bounded vel/accel by caller's factors including default-fallback branches, never scales jerk | CONFIRMED | `ruckig_traj_smoothing.cpp:208-229` (scaling on both bounded and `DEFAULT_*` fallback branches) vs `:230-241` (jerk unscaled) | (uncommitted) |
| `src/ruckig_smoothing.rs:113-125` (consts) | TYPE_B: `DEFAULT_MAX_VELOCITY=5.0`, `DEFAULT_MAX_ACCELERATION=10.0`, `DEFAULT_MAX_JERK=1000.0`, `MAX_DURATION_EXTENSION_FACTOR=50.0`, `DURATION_EXTENSION_FRACTION=1.1`, `OVERSHOOT_CHECK_PERIOD=0.01` transcribed from upstream | CONFIRMED | `ruckig_traj_smoothing.cpp:50-56` — all six constants match exactly | (uncommitted) |
| `src/trajectory_tools.rs:9-19` | TYPE_A: upstream `trajectory_tools` has 5 free functions, 2 ported here, "confirmed line-for-line against `trajectory_tools.cpp:63-76`" | CONFIRMED | `trajectory_tools.cpp:63-76` is exactly `applyTOTGTimeParameterization`+`applyRuckigSmoothing` | (uncommitted) |
| `src/trajectory_tools.rs:21-24` | TYPE_B: `apply_totg_time_parameterization` wraps the scaling-only `compute_time_stamps` overload, matching upstream, which only ever constructs a scaling-only call from here | CONFIRMED | `trajectory_tools.cpp:67-68` calls `totg.computeTimeStamps(trajectory, velocity_scaling_factor, acceleration_scaling_factor)` — the 2-factor overload only | (uncommitted) |
| `src/trajectory_tools.rs:37-43` | TYPE_A: `isTrajectoryEmpty` cited at `cpp:54-57`, `trajectoryWaypointCount` at `cpp:59-62`, `createTrajectoryMessage` at `cpp:78-109` (input has no ROS msg type, only the return type does) | CONFIRMED | All three line ranges match exactly; `createTrajectoryMessage` signature `(vector<string>, trajectory_processing::Trajectory, int) -> trajectory_msgs::msg::JointTrajectory` | (uncommitted) |
| `src/trajectory_tools.rs:50-65` | TYPE_B: `applyTOTGTimeParameterization` (`cpp:63-69`) equals constructing `TimeOptimalTrajectoryGeneration(path_tolerance,resample_dt,min_angle_change)` + `computeTimeStamps(...)`; defaults `0.1/0.1/0.001` | CONFIRMED | `trajectory_tools.cpp:63-69` exact match; defaults at `time_optimal_trajectory_generation.hpp:196-197`, `trajectory_tools.hpp:69-70` | (uncommitted) |
| `src/trajectory_tools.rs:92-101` | TYPE_B: `applyRuckigSmoothing` (`cpp:70-76`) equals constructing `RuckigSmoothing` + `applySmoothing(...)`; defaults `mitigate_overshoot=false`, `overshoot_threshold=0.01` | CONFIRMED | `trajectory_tools.cpp:70-76` exact match; defaults `trajectory_tools.hpp:81-82` | (uncommitted) |
| `src/numeric.rs:17-22` | TYPE_B: `std::min(a,b)`≡`b<a?b:a`, `std::max(a,b)`≡`a<b?b:a`; NaN as 1st arg is returned, NaN as 2nd arg is discarded (asymmetric) | CONFIRMED | Matches the C++ standard's reference-implementation form of `std::min`/`std::max`; tracing NaN through both orderings reproduces exactly the stated asymmetry (independently reasoned from IEEE-754 comparison rules, not inferred from the port) | (uncommitted) |
| `src/numeric.rs:24-26` | TYPE_B: `f64::min`/`f64::max` follow IEEE-754 `minNum`/`maxNum` — NaN discarded regardless of position, propagates only if both are NaN | CONFIRMED | Documented Rust `f64::min`/`f64::max` semantics (IEEE 754-2008 `minNum`/`maxNum`) — independently well-established, contrasts correctly with `cxx_min`/`cxx_max` in the same file | (uncommitted) |
| `src/numeric.rs:28-33` | TYPE_B: `getMinMaxPathAcceleration`'s `std::min(max_path_acceleration, ...)` accumulator starts at `f64::MAX`/`DBL_MAX`, NaN RHS leaves it there | CONFIRMED | `time_optimal_trajectory_generation.cpp:752` (`std::numeric_limits<double>::max()`), `:757-759` (`std::min(max_path_acceleration, ...)`) — read directly; port call site confirmed live at `src/trajectory.rs:751`, not a stale forward-reference | (uncommitted) |
| `src/numeric.rs:35-39` | TYPE_B: `cxx_min`/`cxx_max` reproduce upstream's comparison at `LinearPathSegment::getConfig`, `CircularPathSegment`'s ctor, `getNextAccelerationSwitchingPoint`, `integrateBackward`'s intersection test, every `getMinMax*`/`get*MaxPathVelocity` helper | CONFIRMED | `cpp:74` (getConfig), `:139-140` (Circular ctor), `:487` (getNextAccelerationSwitchingPoint), `:730-731` (integrateBackward), `:758,786,795,807` (the getMinMax*/get*MaxPathVelocity family) — all use `std::min`/`std::max`, none a NaN-symmetric alternative | (uncommitted) |
| `src/path.rs:20-21` | TYPE_B: `DEFAULT_PATH_TOLERANCE = 0.1` matches upstream | CONFIRMED | `time_optimal_trajectory_generation.hpp:52` — `constexpr double DEFAULT_PATH_TOLERANCE = 0.1;` | (uncommitted) |
| `src/path.rs:27-35` | TYPE_A: upstream `path_segments_` is `std::list<std::unique_ptr<PathSegment>>`; port drops upstream's hand-written deep-cloning copy ctor since `Vec<PathSegment>`'s derived `Clone` is equivalent | CONFIRMED | `time_optimal_trajectory_generation.hpp:121` (field decl, `private`, verified directly); `.cpp:292-298` (`Path::Path(const Path&)` clones every segment via `path_segment->clone()`) | (uncommitted) |
| `src/path.rs:48-50` | TYPE_A: `Path::create` returns `std::optional<Path>`; failure reported as `Err` (carrying upstream's `RCLCPP_ERROR` message) rather than bare `None` | CONFIRMED | `time_optimal_trajectory_generation.hpp:92-93`; `.cpp:203-206,208-211,241-246` — every failure path logs `RCLCPP_ERROR` then `return std::nullopt;` | (uncommitted) |
| `src/path.rs:54-60` | TYPE_B: errors on <2 waypoints, `max_deviation<=0`, or 180° turn are checked on un-blended waypoints, before any blend segment is built | CONFIRMED | `time_optimal_trajectory_generation.cpp:203-247` — the 180°-turn check (231-247) runs on raw `waypoints_iterator1/2/3` strictly before `CircularPathSegment` construction at line 251 | (uncommitted) |
| `src/path.rs:71-75,83-84` | TYPE_B: comment paraphrases of upstream's i1/i2/i3-blending rationale and the 180°-turn rejection comment | CONFIRMED | `time_optimal_trajectory_generation.cpp:214-218` and `:231` — close paraphrases of the upstream comments read directly | (uncommitted) |
| `src/path.rs:91` | TYPE_B: `ANGLE_TOLERANCE = 1e-5` transcribed from upstream | CONFIRMED | `time_optimal_trajectory_generation.cpp:240` — `constexpr double angle_tolerance = 1e-05;` | (uncommitted) |
| `src/path.rs:104` | TYPE_B: `0.000_001` threshold for blend `end_config` vs `start_config` transcribed from upstream | CONFIRMED | `time_optimal_trajectory_generation.cpp:255` — `if ((end_config - start_config).norm() > 0.000001)` | (uncommitted) |
| `src/path.rs:135-137` | TYPE_B: the final switching-point entry always marks path end, not a real discontinuity; "upstream drops it the same way" | CONFIRMED | `time_optimal_trajectory_generation.cpp:288` — `path.switching_points_.pop_back();` after the loop | (uncommitted) |
| `src/path.rs:152-156,189-196` | TYPE_A: upstream `getPathSegment` takes `s` by mutable reference and returns the segment pointer (port combines into one return value); `getNextSwitchingPoint` returns arc length and takes `discontinuity` as an out-parameter | CONFIRMED | `time_optimal_trajectory_generation.hpp:117,108`; `.cpp:305-317,337-351` — signatures match as described | (uncommitted) |
| `src/path.rs:296-304` (test comment) | TYPE_B: upstream's own `while (...back().first >= path.length_) pop_back()` cleanup does not remove a same-position tie, only trims points beyond the *new* cumulative length | CONFIRMED | `time_optimal_trajectory_generation.cpp:283-286` — cleanup runs after `path.length_ += segment length` (line 283), so a switching point pushed at the *old* cumulative length is never `>= new length_` and survives, tying with the previously-pushed boundary marker — traced directly, not inferred from the port | (uncommitted) |
| `src/path_segment/mod.rs:11-18` | TYPE_A: nothing in this module is accessible outside the crate — upstream keeps `path_segments_` private and never exposes a `PathSegment*`; `LinearPathSegment`/`CircularPathSegment` are declared only in the `.cpp`, not the header | CONFIRMED | `time_optimal_trajectory_generation.hpp:121` (private field); header declares only `Path`/`PathSegment`/`Trajectory`/`TimeOptimalTrajectoryGeneration` — `LinearPathSegment`/`CircularPathSegment` first appear in `.cpp:63,103` | (uncommitted) |
| `src/path_segment/mod.rs:31-38` | TYPE_A: upstream `PathSegment` is an abstract base with `getConfig`/`getTangent`/`getCurvature`/`getSwitchingPoints` pure virtual and `clone()` for the deep copy, with exactly two subclasses | CONFIRMED | `time_optimal_trajectory_generation.hpp:62-85` (pure virtuals); only `LinearPathSegment`(`.cpp:63`) and `CircularPathSegment`(`.cpp:103`) implement it anywhere in the file | (uncommitted) |
| `src/path_segment/mod.rs:40-49` | TYPE_B: `length`/`position` are upstream's base-class `length_`/`position_`; `position_` is upstream's *public* field, set by `Path::create` only after every segment exists | CONFIRMED | `time_optimal_trajectory_generation.hpp:81` (`double position_;` under `public:`), `:84` (`double length_;` under `protected:`); `.cpp:276` — `path_segment->position_ = path.length_;` inside the post-construction loop | (uncommitted) |
| `src/path_segment/linear.rs:8-10` | TYPE_A: `LinearPathSegment` is defined only in the `.cpp`, not the header — a `Path::create` implementation detail | CONFIRMED | Absent from `time_optimal_trajectory_generation.hpp`; defined at `.cpp:63-101` | (uncommitted) |
| `src/path_segment/linear.rs:18-22` | TYPE_B: `length_` is upstream's base `PathSegment::length_`, set via `PathSegment((end - start).norm())` | CONFIRMED | `time_optimal_trajectory_generation.cpp:66-67` — `LinearPathSegment(...) : PathSegment((end - start).norm()), end_(end), start_(start)` | (uncommitted) |
| `src/path_segment/linear.rs:38-46` | TYPE_B: clamp is `std::max(0.0, std::min(1.0, s))`, not `f64::clamp`; when `length==0`, `s/length` is NaN and upstream's clamp deterministically resolves to `1.0` via the asymmetric NaN rule | CONFIRMED | `time_optimal_trajectory_generation.cpp:73-74` (`s /= length_; s = std::max(0.0, std::min(1.0, s));`) — read directly myself; traced `std::min(1.0, NaN)` → `1.0` (2nd-arg NaN discarded per reference impl `return (b<a)?b:a`, `NaN<1.0` is `false`), then `std::max(0.0, 1.0)` → `1.0` | (uncommitted) |
| `src/path_segment/linear.rs:52-53` | TYPE_B: `getTangent` is constant along the segment; `s` unused, matching upstream's unused `s` parameter | CONFIRMED | `time_optimal_trajectory_generation.cpp:78` — `Eigen::VectorXd getTangent(double /* s */) const override` | (uncommitted) |
| `src/path_segment/circular.rs:25-42` (a) | TYPE_B: `Circular::new` never fails; upstream's constructor has no failure return either | CONFIRMED | `time_optimal_trajectory_generation.cpp:106-148` — `CircularPathSegment(...)` is a `void`-returning ctor; the degenerate case early-returns from the ctor body with `length_=0`, no exception/error path — read directly myself | (uncommitted) |
| `src/path_segment/circular.rs:25-42` (b) | TYPE_B: degenerate-midpoint threshold `0.000001` is a bare literal, distinct from the crate's `EPS` (`1e-6`); upstream never ties them together | CONFIRMED | `time_optimal_trajectory_generation.cpp:109` uses bare `0.000001`; a separate `constexpr double EPS = 1e-6;` exists at `:54`, used elsewhere (`:488-489,730-731`) — value-equal but never the same symbol upstream | (uncommitted) |
| `src/path_segment/circular.rs:25-42` (c) | TYPE_B: parallel/antiparallel degeneracy at `dot > 0.999999` / `dot < -0.999999` (~0.081°), structurally distinct from `Path::create`'s separate 180°-turn rejection (`cos_angle <= -1.0 + 1e-5`) | CONFIRMED | `time_optimal_trajectory_generation.cpp:124` — check local to `CircularPathSegment`'s own ctor, structurally separate from `Path::create`'s check at `.cpp:241` — read directly myself; `acos(0.999999) ≈ 0.0014142 rad ≈ 0.081°` matches | (uncommitted) |
| `src/path_segment/circular.rs:78-85` (comment) | TYPE_B: two independent `>`/`<` comparisons (not `RangeInclusive::contains`) leave a NaN `start_dot_end` resolving to "not degenerate," matching upstream's literal `>`/`<` comparisons | CONFIRMED | `time_optimal_trajectory_generation.cpp:124` — both `start_dot_end > 0.999999` and `start_dot_end < -0.999999` are `false` for NaN under IEEE-754 comparison rules, so the `OR` is false and the degenerate branch is skipped | (uncommitted) |
| `src/lib.rs:6-9,12-21` | TYPE_B: `time_optimal_trajectory_generation.hpp` lines 62-192 are the model-independent `PathSegment`/`Path`/`Trajectory` core, no `robot_model`/`robot_trajectory` dependency | CONFIRMED | `hpp:62` = `class PathSegment`, `:192` = `MOVEIT_CLASS_FORWARD(TimeOptimalTrajectoryGeneration);` (last line before `TimeOptimalTrajectoryGeneration` begins at 193) — boundary exact | (uncommitted) |
| `src/lib.rs:31-34` | TYPE_B: `TimeOptimalTrajectoryGeneration` is "header line 193 on" | CONFIRMED | `hpp:193` — `class TimeOptimalTrajectoryGeneration : public TimeParameterization` | (uncommitted) |
| `src/lib.rs:51-54` | TYPE_B: `MOVEIT_CLASS_FORWARD(TimeParameterization/TimeOptimalTrajectoryGeneration)` "expands to `*Ptr`/`*ConstPtr` `std::shared_ptr` typedefs" | **EXPIRED** | `macros/include/moveit/macros/declare_ptr.hpp`'s `MOVEIT_DECLARE_PTR(Name,Type)` generates **six** typedefs — `Ptr`, `ConstPtr` (shared_ptr), `WeakPtr`, `ConstWeakPtr` (weak_ptr), `UniquePtr`, `ConstUniquePtr` (unique_ptr) — not two; verified by reading the macro body directly | `6ed64e6` |
| `src/lib.rs:58-63` | TYPE_B: `TimeParameterization` — abstract base, 3 pure-virtual `computeTimeStamps` overloads + defaulted special members | CONFIRMED | `time_parameterization.hpp:14-58` — 5 defaulted special members + virtual defaulted dtor, 3 `computeTimeStamps` overloads each `= 0` | (uncommitted) |
| `src/lib.rs:68-76` | TYPE_A: `LimitType`/`LIMIT_TYPES`'s only upstream use is formatting the log-message prefix in `verifyScalingFactor`'s `RCLCPP_WARN` (cited `cpp:1290-1310`) | CONFIRMED | `.cpp:1290-1312` (`verifyScalingFactor`): `LIMIT_TYPES.find(limit_type)` builds `limit_type_str`, used only in the `RCLCPP_WARN` at line 1308; no other use of `LimitType`/`LIMIT_TYPES` found anywhere upstream. Citation end (1310 vs actual 1312) is a 2-line rounding, not a substantive error | (uncommitted) |
| `src/lib.rs:77-93` | TYPE_B: `PathSegment` — fields/methods as listed, pure virtual except `getLength` | CONFIRMED | `.hpp:62-85` — `getLength()` has a concrete body (`return length_;`); `getConfig`/`getTangent`/`getCurvature`/`getSwitchingPoints`/`clone` are all `= 0` | (uncommitted) |
| `src/lib.rs:88-93` | TYPE_B: `LinearPathSegment`/`CircularPathSegment` are not declared in the header at all (`.cpp`-only) | CONFIRMED | Both classes defined only in `.cpp:63,103`; absent from the `.hpp` | (uncommitted) |
| `src/lib.rs:94-99` | TYPE_A: `Path::create` returns `std::optional<Path>`, ported as `Result<Self>` per crate policy | CONFIRMED | `.hpp:92-93` / `.cpp:201` — `static std::optional<Path> create(...)` | (uncommitted) |
| `src/lib.rs:100-102` | TYPE_B: `Path`'s copy constructor subsumed by `#[derive(Clone)]` | CONFIRMED | `.cpp:292-298` — hand-written `Path::Path(const Path&)` deep-clones every segment via virtual `clone()` | (uncommitted) |
| `src/lib.rs:105-111` | TYPE_B: `getNextSwitchingPoint`/`getSwitchingPoints` are `pub(crate)`; "`Trajectory::create` is the only caller ... same as upstream (only `Trajectory::getNextSwitchingPoint` calls the `Path` equivalent)" | **EXPIRED (both halves wrong, one on each side)** | Re-derived: `Path::next_switching_point`'s sole call site in this crate is `trajectory.rs:386`, inside `Trajectory::next_acceleration_switching_point` — **not** `Trajectory::create` as the port-side half claimed. Upstream's `Path::getNextSwitchingPoint`'s sole call site is `.cpp:476`, inside **`Trajectory::getNextAccelerationSwitchingPoint`**, not `Trajectory::getNextSwitchingPoint` (which only calls `getNextAccelerationSwitchingPoint`/`getNextVelocitySwitchingPoint`, `.cpp:421-466`) — **not** `Trajectory::getNextSwitchingPoint` as the upstream-side half claimed. Verified with `rg` for `next_switching_point` in this crate and for `getNextSwitchingPoint`/`getNextAccelerationSwitchingPoint`/`getNextVelocitySwitchingPoint` over the upstream `.cpp`/`.hpp` | `bd5d150` |
| `src/lib.rs:112-114` | TYPE_B: `Path` default constructor is private upstream, "use `create` instead" | CONFIRMED | `.hpp:114-115` — `private: // Default constructor private to prevent misuse. Use \`create\` instead...` — near-verbatim text match | (uncommitted) |
| `src/lib.rs:115-119` | TYPE_B: `getPathSegment` inlined "at each of the four public methods' call sites" | **EXPIRED** | `getPathSegment` has exactly **3** upstream call sites (`getConfig`/`getTangent`/`getCurvature`, `.cpp:321,327,333`), not four — verified myself via `grep -n getPathSegment` on the `.cpp`; `getLength`/`getNextSwitchingPoint`/`getSwitchingPoints` don't call it. The port's own `segment_at` is likewise called from exactly 3 sites (`path.rs:167,173,179`), so the code matches upstream's real count of 3 — only the doc's "four" is wrong on both sides | `d2e5070` |
| `src/lib.rs:144-150` | TYPE_A: `getTrajectorySegment`'s `cached_time_`/`cached_trajectory_segment_` is a forward-scan cache, dropped in the port | CONFIRMED | `.cpp:841-862` — resets to `begin()` when `time < cached_time_`, then `while (time >= cached_trajectory_segment_->time_) ++cached_trajectory_segment_;`, a forward linear scan | (uncommitted) |
| `src/lib.rs:165-166` | TYPE_A: `computeTimeStamps(JointLimits vector)` (D1) is a thin wrapper around the map overload | CONFIRMED | `.cpp:1006-1027` — builds `velocity_limits`/`acceleration_limits` maps then calls the map overload directly | (uncommitted) |
| `src/lib.rs:170-176` | TYPE_A: `hasMixedJointTypes`'s only upstream use is gating a dropped `RCLCPP_WARN` inside `doTimeParameterizationCalculations` | CONFIRMED | Exactly 2 upstream hits for `hasMixedJointTypes`: declaration/definition and the sole call at `.cpp:1176`, gating `RCLCPP_WARN` at 1178-1180; no other callers (cross-verified independently in the parity-test agent's audit above) | (uncommitted) |
| `src/lib.rs:180-184` | TYPE_A: `totgComputeTimeStamps` has "one upstream call-ordering bug (a discarded first-call failure)" | CONFIRMED | `.cpp:1153-1154` — `default_totg.computeTimeStamps(...)`'s return value is discarded; `trajectory.getDuration()` is read unconditionally right after, even on failure | (uncommitted) |
| `src/lib.rs:191-201` | TYPE_A: `trajectory_tools.hpp`'s `isTrajectoryEmpty`/`trajectoryWaypointCount`/`createTrajectoryMessage` are D1 (`moveit_msgs`/`trajectory_msgs` types) | CONFIRMED | `trajectory_tools.hpp:50,56,87-89` — all three signatures carry `moveit_msgs::msg::RobotTrajectory` or return `trajectory_msgs::msg::JointTrajectory` | (uncommitted) |
| `src/lib.rs:212-213` | TYPE_A: `RuckigSmoothing::applySmoothing(JointLimits vector)` (D1) is a thin wrapper | CONFIRMED | `ruckig_traj_smoothing.cpp:154-180` — builds velocity/acceleration/jerk maps then calls the map overload | (uncommitted) |
| `src/lib.rs:213-219` | TYPE_A: `getRobotModelBounds` is `[[nodiscard]] bool` upstream, made infallible in the port | CONFIRMED | `ruckig_traj_smoothing.hpp:109-112` — `[[nodiscard]] static bool getRobotModelBounds(...)` | (uncommitted) |
| `src/lib.rs:226-229` | TYPE_B: `extendTrajectoryDuration` has a header/`.cpp` doc-comment mismatch, resolved by following the `.cpp` | CONFIRMED | Header (`ruckig_traj_smoothing.hpp:147-159`) names the 2nd parameter `num_waypoints` ("Number of waypoints in the trajectory"); the `.cpp` definition (`:338-341`) and call site (`:312`, passing `waypoint_idx`) use it as an index — a genuine upstream header/impl mismatch, independently cross-confirmed by the ruckig_smoothing.rs agent above | (uncommitted) |
| `src/lib.rs:283-287` | TYPE_B: `tests/large_accel.rs` ports upstream `testLargeAccel` | CONFIRMED | `test_time_optimal_trajectory_generation.cpp:243` — `TEST(time_optimal_trajectory_generation, testLargeAccel)` | (uncommitted) |
| `src/lib.rs:288-296` | TYPE_B: crate's own `#[cfg(test)]` covers every upstream TOTG gtest case, naming `test1`/`test2`/`test3`/`testSingleDofDiscontinuity`/`testCustomLimits` "and friends" | CONFIRMED | `test_time_optimal_trajectory_generation.cpp` `TEST(...)` names at lines 70,112,160,209,243,326,362,375,414,534,563,613,623,633 include all five named plus others | (uncommitted) |
| `src/lib.rs:300-304` | TYPE_B: `tests/ruckig_smoothing.rs` ports every upstream `RuckigTests` case | CONFIRMED | `test_ruckig_traj_smoothing.cpp` has exactly 4 `TEST_F(RuckigTests, ...)` cases (`basic_trajectory`, `basic_trajectory_with_custom_limits`, `trajectory_duration`, `single_waypoint`) | (uncommitted) |
| `src/lib.rs:305-312` | TYPE_B: `tests/robot_trajectory.rs` ports every `test_robot_trajectory.cpp` case "except the four named in that file's own header comment" | **EXPIRED** | `tests/robot_trajectory.rs:8-14`'s own header comment names **five** excluded tests — `RobotTrajectoryShallowCopy`, `ChainEdits`, `DoubleReverse`, `MultiDofTrajectoryToJointStates`, `SetMultiDofTrajectory` (verified by reading the header directly) — all confirmed to exist upstream. `lib.rs`'s own prose says "four" while its own parenthetical lists five — an internal inconsistency in the port's doc comment itself, not just a stale citation | (uncommitted) |
| `src/lib.rs:344-352` | TYPE_A: `RobotTrajectory::print`/`operator<<` ported as `impl Display` (round 12); `Display` has no `variable_indexes` override | CONFIRMED | `robot_trajectory.hpp:402` — `print(std::ostream&, std::vector<int> variable_indexes = {})`; `Display::fmt`'s fixed signature has no room for it | (uncommitted) |
| `src/lib.rs:370-372` | TYPE_B: `MOVEIT_CLASS_FORWARD(RobotTrajectory)` "expands to `RobotTrajectoryPtr`/`ConstPtr`/`WeakPtr`" | **EXPIRED** | Same `MOVEIT_DECLARE_PTR` macro as `lib.rs:51-54` above generates 6 typedefs, not 3 — missing `ConstWeakPtr`, `UniquePtr`, `ConstUniquePtr` | `6ed64e6` |
| `src/lib.rs:374-381` / `src/robot_trajectory.rs:31-37` | TYPE_A: unknown group name in `RobotTrajectory(model, group: string)`/`for_group_name` is `Err`, not upstream's silent whole-robot fallback (`getJointModelGroup` logs + returns `nullptr`) | CONFIRMED | `robot_trajectory.cpp:64-67` — ctor: `group_(group.empty() ? nullptr : robot_model->getJointModelGroup(group))`; `robot_model.cpp:512-521` — unknown name → `RCLCPP_ERROR` + `return nullptr` | (uncommitted) |
| `src/lib.rs:382-386` / `src/robot_trajectory.rs:24-30` | TYPE_A: upstream `operator=`/`(other, deepcopy=false)` alias waypoints (shallow copy); `#[derive(Clone)]` always deep-copies since `RobotState` is a plain value type | CONFIRMED | `robot_trajectory.hpp:79-86` — `operator=` defaulted (shallow); `RobotTrajectory(other, bool deepcopy = false)` defaults to `false` (shallow/aliasing) | (uncommitted) |
| `src/lib.rs:392-398` | TYPE_B: `size()` differs from `getWayPointCount()` only by a debug-mode `assert` that the two deques stay the same length | CONFIRMED | `robot_trajectory.hpp:106-115` — `size()` adds `assert(waypoints_.size() == duration_from_previous_.size());` over `getWayPointCount()` | (uncommitted) |
| `src/lib.rs:399-405` / `src/robot_trajectory.rs:52-57` | TYPE_A: `getWayPoint`/`getWayPointPtr`/`getFirst`/`LastWayPoint(Ptr)` are unchecked upstream (UB out of range); `removeWayPoint`/`insertWayPoint` index unchecked too | CONFIRMED | `robot_trajectory.hpp:117-145` (direct `waypoints_[index]`/`.front()`/`.back()`, no bounds check), `:219-230` (`insertWayPoint`, unchecked), `:251-256` (`removeWayPoint`, unchecked `.erase`) | (uncommitted) |
| `src/lib.rs:427-430` / `src/robot_trajectory.rs:270-279` | TYPE_A: `addPrefixWayPoint`'s upstream `dt` parameter is dropped; upstream actually stores whatever `dt` the caller passes at waypoint 0 | CONFIRMED | `robot_trajectory.hpp:206-217` — `addPrefixWayPoint(state, dt)` does `duration_from_previous_.push_front(dt);`, an arbitrary caller value stored at index 0 | (uncommitted) |
| `src/lib.rs:434-439` / `src/robot_trajectory.rs:330-338` | TYPE_B: `append`'s `start_index=0`/`end_index=SIZE_MAX` defaults dropped (no Rust default params); `end_index` clamped, empty range silent no-op; does not call `update()`, matching upstream | CONFIRMED | `robot_trajectory.hpp:242-243` (`end_index` default `std::numeric_limits<std::size_t>::max()`); `.cpp:132-147` — `end_index = std::min(...)`, early return on `start_index >= end_index`, no `->update()` call anywhere in the function | (uncommitted) |
| `src/lib.rs:440-446` | TYPE_A: `swap` dropped; upstream's own `swap` exists specifically to call `robot_model_.swap(...)` on the `shared_ptr` rather than an atomic-refcount copy/decrement pair | CONFIRMED | `.cpp:124-130` — `robot_model_.swap(other.robot_model_); std::swap(group_, ...); waypoints_.swap(...); duration_from_previous_.swap(...);` — uses `shared_ptr::swap`, not reassignment | (uncommitted) |
| `src/lib.rs:463-469` / `src/robot_trajectory.rs:527-530` | TYPE_B: `findWayPointIndicesForDurationAfterStart`'s 4 documented edge cases (empty, negative duration, past-total, single-waypoint) are all reproduced | CONFIRMED | `robot_trajectory.hpp:314-322` documents exactly these 4 cases with the stated `(before, after, blend)` values; `.cpp:568-601` implements them consistently | (uncommitted) |
| `src/robot_trajectory.rs:38-51` | TYPE_A: new invariant `duration_from_previous[0] == 0.0`; upstream's `getAverageSegmentDuration` comment "if the initial segment has a duration of 0, exclude it" is a "tacit admission" it's expected zero but never enforced | CONFIRMED | `.cpp:109-121` — `// If the initial segment has a duration of 0, exclude it from the average calculation` (near-verbatim); no enforcement exists anywhere upstream that index 0 must be zero (`setWayPointDurationFromPrevious`, `addSuffixWayPoint`, `insertWayPoint`, `append` all accept arbitrary values at index 0 unchecked) | (uncommitted) |
| `src/robot_trajectory.rs:58-64` | TYPE_A: `getStateAtDurationFromStart` returns `bool`/out-param upstream, `false` only when trajectory empty | CONFIRMED | `.cpp:616-630` — `if (getWayPointCount() == 0) return false;` is the only failure path; otherwise always `return true` after interpolating | (uncommitted) |
| `src/robot_trajectory.rs:65-74` | TYPE_B: `Display` always takes upstream's `variable_indexes.empty()` default branch; column order (index, time, pos, vel, acc, eff, each conditional) matches upstream exactly | CONFIRMED | `.cpp:632-701` (`print`) — `if (variable_indexes.empty()) {...}`, per-waypoint `pos`/conditional `vel`/`acc`/`eff` in that order; `operator<<` (`.cpp:703-707`) always calls `print(out)` with the default empty `variable_indexes` | (uncommitted) |
| `src/robot_trajectory.rs:66-71` | TYPE_A: Rust `Display` skips reproducing `std::ios`'s persistent `std::fixed << std::setprecision(3)` flag state and its restore | CONFIRMED | `.cpp:641-643,697-700` — saves `out.flags()`/`out.precision()`, sets `std::fixed`/`setprecision(3)`, restores both at function end; no `Display::fmt` equivalent exists | (uncommitted) |
| `src/robot_trajectory.rs:388-395` | TYPE_B: `reverse` inverts velocity via `invert_velocity` (no-op if unset), matching upstream `invertVelocity()`; acceleration deliberately untouched despite living inside `reverse()` | CONFIRMED | `robot_state.cpp:477-484` — `invertVelocity()` only negates `velocity_[i]` under `if (has_velocity_)`, no acceleration field touched; `robot_trajectory.cpp:149-165` (`reverse`) calls only `invertVelocity()` | (uncommitted) |
| `src/robot_trajectory.rs:414-417` | TYPE_B: `unwind()` unwraps continuous joints so consecutive waypoints never jump more than `PI`, rather than wrapping at `[-PI, PI]` | CONFIRMED | `.cpp:167-207` — `if (last_value > current_value + M_PI) running_offset += 2*M_PI; else if (...) running_offset -= 2*M_PI;` matches the "never jump more than PI" running-offset logic, including the `group_` vs `getContinuousJointModels()` selection | (uncommitted) |
| `src/robot_trajectory.rs:581-584` | TYPE_B: `interpolate_into` matches `RobotState::interpolate`'s group-less overload: linear interpolation over every active (non-mimic, non-fixed) joint of the whole model | CONFIRMED | `robot_state.cpp:1138-1145` delegates to `RobotModel::interpolate` (`robot_model.cpp:1518-1530`), commented "we interpolate values only for active joint models (non-mimic)", iterates `active_joint_model_vector_` | (uncommitted) |
| `src/robot_trajectory.rs:610-612` | TYPE_B: `distance` matches `RobotState::distance`'s group-less overload: L1 sum of `distance_factor * joint.distance(...)` over active joints | CONFIRMED | `robot_state.hpp:1354-1358` delegates to `RobotModel::distance` (`robot_model.cpp:1506-1516`) — `d += active_joint_model_vector_[i]->getDistanceFactor() * ...->distance(...)` | (uncommitted) |
| `src/robot_trajectory.rs:210-218` | TYPE_B: `way_point_duration_from_start` clamps out-of-range `index` to the last waypoint, matching upstream's own doc | CONFIRMED | `robot_trajectory.hpp:152-155` doc comment quoted verbatim; `.cpp:603-614` — `if (index >= duration_from_previous_.size()) index = duration_from_previous_.size() - 1;` | (uncommitted) |
| `src/robot_trajectory.rs:220-227` | TYPE_B: `way_point_duration_from_previous` returns `0.0` if `index` out of range, matching upstream | CONFIRMED | `robot_trajectory.hpp:158-168` — `if (duration_from_previous_.size() > index) return duration_from_previous_[index]; else return 0.0;` | (uncommitted) |
| `src/trajectory.rs:5-7` | TYPE_B: port pinned to upstream `time_optimal_trajectory_generation.hpp`(class `Trajectory`)/`.cpp`(`Trajectory::` methods) | CONFIRMED | Both files exist at those relative paths; `hpp:124` declares `class Trajectory`, `.cpp:358+` implements `Trajectory::create` etc. | (uncommitted) |
| `src/trajectory.rs:29-31` | TYPE_B: `EPS = 1e-6` matches upstream's anonymous-namespace `EPS` | CONFIRMED | `cpp:54` — `constexpr double EPS = 1e-6;` | (uncommitted) |
| `src/trajectory.rs:33-45` | TYPE_A: `VELOCITY_SWITCHING_SCAN_STEP = 1e-3` mirrors upstream's anonymous-namespace `DEFAULT_TIMESTEP = 1e-3`, kept as a constant distinct from the header's `time_step = 0.001` default argument | CONFIRMED | `cpp:53` — `constexpr double DEFAULT_TIMESTEP = 1e-3;`; `hpp:130` — `double time_step = 0.001` — two separate symbols upstream, coincidentally equal (this framing is the correct one — see the contradicting `time_optimal_trajectory_generation.rs:332-338` row below, which misattributes the *same* `DEFAULT_TIMESTEP` symbol as two independently-chosen values) | (uncommitted) |
| `src/trajectory.rs:74-81` | TYPE_A: `end_trajectory_` is write-only upstream — assigned on both `integrateBackward` failure paths (`hpp:184`), no getter, no other reader anywhere in moveit2 | CONFIRMED | `rg end_trajectory_` across `/home/stevek/work/moveit2` returns exactly 3 hits: `hpp:184` (decl), `.cpp:715` and `.cpp:744` (the two assignments) — no reads anywhere | (uncommitted) |
| `src/trajectory.rs:82-89` | TYPE_A: `cached_time_`/`cached_trajectory_segment_` mutable forward-scan cache in `getTrajectorySegment`; dropping it is behaviour-preserving since `trajectory_` is time-sorted | CONFIRMED | `hpp:188-189` declares both `mutable`; `.cpp:841-862` `getTrajectorySegment` implements exactly the described cache-or-fast-path scan | (uncommitted) |
| `src/trajectory.rs:104-106` | TYPE_B: `Trajectory::create` returns `std::optional<Trajectory>` upstream | CONFIRMED | `hpp:129` | (uncommitted) |
| `src/trajectory.rs:138-143` | TYPE_B: `create()`'s loop condition rechecks `valid_` "a third time" after `integrateForward` returns, redundantly, since every `valid_=false` path in `integrateForward` also returns `true` | CONFIRMED (substance); imprecise wording | `.cpp:606-609` and `:625-629` both set `valid_ = false` then `return true`, so the recheck can never differ. The ordinal "third" is unclear — the cited `while` expression (`cpp:370`) only shows `valid_` twice; no earlier third check is visible in `create()` | (uncommitted) |
| `src/trajectory.rs:205-224` | TYPE_B: `getVelocity` is a step function of `time` within a segment — always uses the full segment time-step, never re-derives against the query `time` | CONFIRMED | `.cpp:881-896` — no reference to the raw `time` parameter anywhere in the body, only `it->time_ - previous->time_` | (uncommitted) |
| `src/trajectory.rs:232-244` | TYPE_B: `getAcceleration` has the same non-re-derivation property, for the same reason (constant accel per Euler segment) | CONFIRMED | `.cpp:898-916`, identical shape to `getVelocity`; `if (time_step > 0.0) path_acc /= time_step;` (`:913-914`) matches the port exactly | (uncommitted) |
| `src/trajectory.rs:261-296` | TYPE_B: `getPosition`'s quadratic fit uses the segment's own time-step for the acceleration coefficient but evaluates position at query `time` (unlike velocity/acceleration) | CONFIRMED | `.cpp:864-878` — `time_step` reused for the accel coefficient at 870, then reassigned to `time - previous->time_` at 874 before the final position evaluation | (uncommitted) |
| `src/trajectory.rs:298-314` | TYPE_A: `segment_index`/upstream `getTrajectorySegment` — binary search over a time-sorted list finds the same index as the cached forward scan / `time >= trajectory_.back().time_` fast path | CONFIRMED | `.cpp:841-862` | (uncommitted) |
| `src/trajectory.rs:551-556` | TYPE_B: "Avoid a TrajectoryStep with path_pos near a switching point..." comment citing `moveit/moveit#1665`, transcribed from upstream | CONFIRMED verbatim | `.cpp:588-589`, including the exact GitHub issue number | (uncommitted) |
| `src/trajectory.rs:636-651` | TYPE_A: upstream dereferences `next_discontinuity` unconditionally here (no `!= switching_points.end()` guard), technically UB with `std::list`'s end sentinel; port instead treats "no next discontinuity" as "cannot be past one" | CONFIRMED | `.cpp:668` — `if (after > next_discontinuity->first)` has no end-iterator guard, unlike the analogous check at `.cpp:586` (`next_discontinuity != switching_points.end() && ...`) | (uncommitted) |
| `src/trajectory.rs:678-690` | TYPE_A: `Vec::push` + reverse-at-return is behaviourally equivalent to upstream's `std::list::push_front`, since upstream's `trajectory.front()` always names the most-recently-added (smallest `path_pos`) element | CONFIRMED | Traced `.cpp:690-745`: each iteration `push_front`s a smaller `path_pos`, so `front()` = smallest/most-recent; final `splice(end(), trajectory)` appends it in ascending order, matching the port's reverse-then-extend | (uncommitted) |
| `src/trajectory.rs:815-816` | TYPE_B: `acceleration_max_path_velocity_deriv` is a central-difference numerical derivative of `acceleration_max_path_velocity` | CONFIRMED | `.cpp:812-816`, literal `(f(p+EPS) - f(p-EPS)) / (2*EPS)` | (uncommitted) |
| `src/trajectory.rs:823-826` | TYPE_B: `velocity_max_path_velocity_deriv` is the *analytic* derivative of `velocity_max_path_velocity`, taken along the active/binding velocity-constraint joint | CONFIRMED | `.cpp:818-834` — `active_constraint` = argmin joint, analytic formula matches exactly | (uncommitted) |
| `src/trajectory.rs:855-877` | TYPE_B: `upstream_test1` uses `EXPECT_DOUBLE_EQ` (not `EXPECT_NEAR`) for duration/position, bit-identical (0 ULP) to upstream; the two `epsilon=0.1` velocity checks are `EXPECT_NEAR(0.0,…,0.1)` transcribed verbatim from upstream lines 108/109 | CONFIRMED | `.cpp:92-104` all `EXPECT_DOUBLE_EQ`; bit-check confirms `40.080_256_821_829_85` (Rust) and `40.080256821829849` (upstream) parse to the identical f64 bit pattern; `.cpp:108-109` exact | (uncommitted) |
| `src/trajectory.rs:910-936` | TYPE_A/B: `upstream_test2` — position bit-identical (0 ULP); duration NOT bit-exact, measured diff `8.893039193935692e-9` (actual `1922.14184275348748` vs literal `1922.1418427445944`); `epsilon=1e-6` kept, `max_relative` pinned to `f64::EPSILON` | CONFIRMED | Sub-agent's own scratch probe against the real crate's `Trajectory::create` reproduced `duration = 1922.14184275348748088`, diff `= 8.893039193935692e-9`, matching the doc's cited figures to every printed digit | (uncommitted) |
| `src/trajectory.rs:938-1097` | TYPE_B: "root cause" essay's upstream line-range citations — `integrateForward` cpp:564-663, `integrateBackward` cpp:690-742, `getMinMax*`/`get*MaxPathVelocity*` helpers cpp:747-828 (timing loop cpp:398-410 correct) | **EXPIRED** | Verified myself directly (grepped for the `Trajectory::integrateBackward`, `Trajectory::getMinMaxPathAcceleration`, and `Trajectory::getVelocityMaxPathVelocityDeriv` signature lines, plus reading the closing braces): `integrateForward`'s real span is `cpp:564-688`, not 564-663 (misses ~25 lines, including the `.cpp:680` aliasing expression `trajectory.rs`'s own `integrate_forward` doc cites elsewhere); `integrateBackward`'s real span is `cpp:690-745`, not 690-742; helper span is `cpp:747-834`, not 747-828 (misses the tail of `getVelocityMaxPathVelocityDeriv`) | (uncommitted) |
| `src/trajectory.rs:1159-1195` | TYPE_A: `getNextVelocitySwitchingPoint`, `getVelocityMaxPathVelocity(Deriv)`, and `trajectory_` are all `private` on upstream's `Trajectory` (header lines 143-183); `Path::getPathSegment` is private; `CircularPathSegment` is declared in the `.cpp`, not the header | CONFIRMED | `hpp` `private:` section spans 143-190 and includes exactly those symbols; `CircularPathSegment` confirmed only at `.cpp:103`; `Path::getPathSegment` private at `hpp:117` | (uncommitted) |
| `src/trajectory.rs:1203-1206` | TYPE_B: `upstream_test2`'s `epsilon=0.1` velocity checks transcribed verbatim from upstream lines 156/157 | CONFIRMED | `.cpp:156-157` exact | (uncommitted) |
| `src/trajectory.rs:1248-1265` | TYPE_B: `upstream_test3` exercises upstream's default `time_step=0.001` (`hpp:130`); duration IS bit-identical (0 ULP), unlike `test2`; velocity checks are upstream lines 204/205 | CONFIRMED | `.cpp` `test3` (line 184) omits the `time_step` arg, relying on the `hpp:130` default; bit-check confirms `1_919.559_788_881_297_4`/`1919.5597888812974` parse identically; lines 204-205 exact | (uncommitted) |
| `src/trajectory.rs:1302-1309` | TYPE_B: `max_acceleration` literal `0.78539816339699997` is "4550 ULPs" from `FRAC_PI_4` | **EXPIRED (numerically wrong)** | A compiled Rust probe comparing the literal against `std::f64::consts::FRAC_PI_4` gives `-4038` ULP, not `-4550` — independently corroborated by the separate `tests/large_accel.rs:20-24` agent finding the identical wrong figure (same literal duplicated in that file) | (uncommitted) |
| `src/trajectory.rs:1349-1381` | TYPE_A/B: `upstream_test_single_dof_discontinuity` — `getPosition(0.0)[0]` is upstream's only `EXPECT_DOUBLE_EQ` in the test (bit-identical, 0 ULP); velocity checks are lines 594/595; duration (line 589) and acceleration (604/608) both keep upstream's own `1e-3`; round-12 bisection found duration fails already at `1e-6` so `1e-3` kept, acceleration passes at `1e-9`/fails at `1e-12` so tightened to `1e-9` | CONFIRMED (qualitative); illustrative ULP-adjacent figure UNVERIFIABLE(sub-agent's independent re-sweep found the worst-case acceleration deviation to be `~6-8e-11`, not the doc's cited `1.78e-12` — both still fail at `1e-12`/pass at `1e-9` so the qualitative bisection-ladder conclusion holds, but the specific illustrative number could not be reproduced with a differently-enumerated sample sweep) | `rg EXPECT_DOUBLE_EQ` over the test confirms line 592 is the only hit; lines 589/594/595/604/608 all match exactly; duration probe reproduced `0.32040131148497680`, matching the cited actual to every digit | (uncommitted) |
| `src/trajectory.rs:1420-1449` | TYPE_B: `upstream_test_relevant_zero_max_accelerations_invalidate_trajectory`/`_irrelevant_..._dont_invalidate_trajectory`/`_time_step_zero_makes_trajectory_invalid` ported verbatim | CONFIRMED | `.cpp:613-621`, `:623-631`, `:633-638` respectively | (uncommitted) |
| `src/trajectory.rs:1507-1538` | TYPE_B (negative claim): zero-length-path NaN-duration test — the same 0/0 divide, the same NaN-discarding `std::min`/`std::max`, and the same zero-over-zero timing-loop divide "exist verbatim in upstream's C++"; no length-zero guard exists in upstream `Path::create`/`Trajectory::create` | CONFIRMED | `.cpp:405` timing-loop formula textually identical to the port's; `rg` over `.cpp:201-411` finds no zero-length guard in either `Path::create` or `Trajectory::create`; `std::min`'s standard second-argument-wins-on-NaN behaviour matches the claimed `cxx_min` semantics | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:21-31` | TYPE_A: `computeTimeStamps(...,vector<JointLimits>&,...)` is a thin wrapper unpacking the msg into the same maps `compute_time_stamps_with_limits` takes | CONFIRMED | `cpp:1006-1027` — builds `velocity_limits`/`acceleration_limits` maps from `joint_limits`, then calls the map overload | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:32-35` | TYPE_A: every upstream `RCLCPP_ERROR`+`return false` site still returns `Error` here (logging itself dropped) | CONFIRMED | All `RCLCPP_ERROR`+`return false` sites in `totg.cpp` (934/964/973/985/994/1041/1081/1092/1115/1125/1149/1172/1233/1240) have a matching `Error::other`/`?` in the port; the two non-returning log sites (948, 1055) correctly have no Rust analogue since the claim is scoped to "+ return false" sites | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:42-51` | TYPE_B: `moveit-model`'s URDF loader never sets `acceleration_bounded`, matching upstream's `jointBoundsFromURDF`, which never reads an acceleration limit from URDF `<limit>` | CONFIRMED | `robot_model.cpp:895-927` — `jointBoundsFromURDF` only touches `position`/`velocity` fields; no `acceleration` anywhere in the function | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:59-69` | TYPE_A: `RobotModel::joint_model_mut` mirrors upstream's non-`const` `getJointModel(const std::string&)` (`robot_model.hpp:146`), the same accessor `joint_limits.yaml` loaders use via `JointModel::setVariableBounds` (`joint_model.hpp:356/359`) post-construction | CONFIRMED | `robot_model.hpp:146` — `JointModel* getJointModel(const std::string& joint);`; `joint_model.hpp:356/359` — the two `setVariableBounds` overloads; `robot_model_loader.cpp:246` calls `joint_model->setVariableBounds(joint_limit)` post-construction | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:96-104` | TYPE_A/B: `moveit_state::dynamics::DynamicsSolver` is "already ported" (RNE recursion written out, not delegated to KDL) | CONFIRMED | Upstream `dynamics_solver.cpp:38-41,112,151` delegates to `KDL::ChainIdSolver_RNE`; `crates/moveit-state/src/dynamics.rs:419+` implements `DynamicsSolver`/`torques`/`max_torques`/`max_payload`/`payload_torques` directly | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:106-113` | TYPE_A: no `getMaxAcceleration`-shaped method anywhere in `DynamicsSolver`, upstream or here | CONFIRMED | `dynamics_solver.hpp` public surface: `getTorques`, `getMaxPayload`, `getPayloadTorques` only | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:113-119` | TYPE_A: "an `rg` sweep for `dynamics_solver`/`DynamicsSolver` across `moveit_core` outside `dynamics_solver/` returns no hits" | **EXPIRED (imprecise)** | Actual sweep returns 6 hits outside `dynamics_solver/`: `CMakeLists.txt:55,83,108` and `CHANGELOG.rst:1165,1612,2018` — none are code call-sites into `trajectory_processing`/`robot_trajectory`, so the substance ("no wiring into this crate's domain") holds, but the literal "no hits" is false | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:123-136` | TYPE_A: upstream's `TimeOptimalTrajectoryGeneration` is the sole implementor of `TimeParameterization`; `RuckigSmoothing` does not inherit it | CONFIRMED | `rg 'public TimeParameterization'` over the whole checkout: exactly 1 hit, `hpp:193`; `ruckig_traj_smoothing.hpp:45-46` — `class RuckigSmoothing` has no base class | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:137-143` | TYPE_A: no call site anywhere in upstream takes `TimeParameterization&`/`Ptr`/holds one in a container; no pluginlib export | CONFIRMED | `rg` for `TimeParameterization` followed by `&`, `Ptr`, or `Const` outside its own header + totg header: 0 hits; `rg 'PLUGINLIB_EXPORT_CLASS.*TimeParameterization'`: 0 hits | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:153-160` | TYPE_A: third pure-virtual overload takes `const vector<moveit_msgs::msg::JointLimits>&` at `time_parameterization.hpp:55-58` | CONFIRMED | Read directly, exact signature and line range | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:168-182` | TYPE_A: upstream ctor defaults `path_tolerance`/`resample_dt`/`min_angle_change`; all three `computeTimeStamps` overloads default both scaling factors to `1.0`; `TotgOptions::default()` reproduces `DEFAULT_PATH_TOLERANCE`(=0.1)/`0.1`/`0.001`/`1.0`/`1.0` | CONFIRMED | `hpp:52` `DEFAULT_PATH_TOLERANCE = 0.1`; `hpp:196` ctor defaults `(DEFAULT_PATH_TOLERANCE, 0.1, 0.001)`; `hpp:213-214,235-236,255-256` all default both scaling factors to `1.0` | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:183-189` | TYPE_A: `getVariableIndexList()`/`getVariableNames()` (global per-DOF index into the whole `RobotState`) replaced by iterating `JointModelGroup::variable_names` + name-based `RobotState` access — same substitution `ruckig_smoothing` already made | CONFIRMED | `joint_model_group.hpp:285-289` — "Get the index locations in the complete robot state"; identical claim independently confirmed for `ruckig_smoothing.rs:68-75` above | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:190-224` | TYPE_A: the two overloads build the active-limit vector inconsistently — scaling-only overload correctly writes by loop position; explicit-limits overload rebinds `idx` to group-local positions (which include mimic-joint slots) and writes into a vector sized `indices.size()` — OOB for any mimic-joint group, UB in C++ | CONFIRMED | `cpp:924-1004` (scaling-only), `cpp:1029-1135` (explicit-limits), all cited lines (954,957,968-969 vs 1051-1053,1058-1060,1062,1072/1085/1106/1119) read verbatim and match; `computeJointVariableIndices` (`joint_model_group.cpp:621-644`) confirmed to return group-local positions including mimic slots (`joint_model_group.hpp:188-189`) | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:225-247` | TYPE_A: `doTimeParameterizationCalculations` (`cpp:1162-1271`) builds waypoints over every group variable incl. mimic against limit vectors sized to active-only count — another unchecked OOB `Eigen::VectorXd` index for mimic groups; port checks lengths and returns `Error::other` instead | CONFIRMED | All cited lines verified exact; `getVariableCount()` doc (`joint_model_group.hpp:408-410`) — "includes variables necessary for mimic joints" | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:248-256` | TYPE_A: `hasMixedJointTypes`'s only upstream use (`cpp:1176-1180`) is `RCLCPP_WARN`, never gates control flow; ported as a standalone predicate but not called from `do_time_parameterization_calculations` | CONFIRMED | `cpp:1176-1180` — `if (hasMixedJointTypes(group)) { RCLCPP_WARN(...); }`, no return/branch, falls through unconditionally (independently cross-confirmed in two other agents' reports above) | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:257-268` | TYPE_A: bound-validation comparison operator differs by overload — scaling-only rejects `max_velocity_ <= 0.0` but `max_acceleration_ < 0.0`; explicit-limits rejects `max_velocity_ < 0.0` (not `<=`) for its fallback and `max_acceleration_ < 0.0` for its own; a caller-supplied entry is never validated | CONFIRMED | `cpp:962/983` and `cpp:1079/1113`, `cpp:1069-1074/1103-1108` all read verbatim, match exactly | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:269-273` | TYPE_A: upstream's out-of-range error messages name the wrong variable — `vars[idx]` instead of `vars[active_joint_indices[idx]]` (`cpp:964-965`), a cosmetic copy-paste bug the port drops | CONFIRMED | `cpp:965` — `vars[idx].c_str()` in the `RCLCPP_ERROR` call, while the bound itself was fetched via `vars[active_joint_indices[idx]]` at `cpp:957` — a genuine upstream inconsistency | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:274-287` | TYPE_A: upstream ctor stores `resample_dt` as-is (`cpp:918-920`); `doTimeParameterizationCalculations` narrows duration/resample_dt from `double` to `size_t` with an unchecked cast (`cpp:1245`), UB in C++ for out-of-range/negative input; Rust's `as usize` is a defined saturating cast, converting that UB into two distinct silently-wrong outcomes | CONFIRMED | `cpp:918-922` ctor stores members verbatim, no validation; `cpp:1245` — `const size_t sample_count = std::ceil(parameterized->getDuration() / resample_dt_);`, an implicit double→size_t narrowing conversion, UB per the C++ standard for out-of-range values; `as usize` float cast is documented-saturating since Rust 1.45 | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:308-319` | TYPE_A: `MAX_RESAMPLE_SAMPLE_COUNT` bound is not redundant with the ctor check (duration only known at consumption time); "upstream has none" | CONFIRMED | `cpp:1245-1268` — `sample_count` computed and used directly in the resample loop with no cap anywhere in the function | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:332-338` | TYPE_A: `DEFAULT_TIMESTEP` (`cpp:53`) is distinct from `crate::trajectory::VELOCITY_SWITCHING_SCAN_STEP`; "**they share a value only because both upstream authors independently picked `1e-3`**" | **EXPIRED** | Verified myself directly: upstream defines `DEFAULT_TIMESTEP` exactly **once** (`cpp:53`, anonymous namespace) and reuses that single symbol both as the switching-point scan step (`cpp:522,525,541`, the function `VELOCITY_SWITCHING_SCAN_STEP` ports) **and** as the argument passed to `Trajectory::create` (`cpp:1237`, the value this file's own `DEFAULT_TIMESTEP` ports). One constant reused at two call sites, not two independently-chosen values — this directly contradicts (and is contradicted by) `trajectory.rs:33-45`'s own doc comment, which correctly attributes `VELOCITY_SWITCHING_SCAN_STEP` to the same upstream `DEFAULT_TIMESTEP` symbol. **This is the one EXPIRED claim in this file most likely to need a doc fix**, since it makes an affirmatively false statement about upstream's authorship rather than just an imprecise citation | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:341-342` | TYPE_A: `DEFAULT_SCALING_FACTOR`, `cpp:55` | CONFIRMED | `cpp:55` — `constexpr double DEFAULT_SCALING_FACTOR = 1.0;` | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:423-424,505-506` | TYPE_A: function-boundary citations — scaling-only overload = `cpp:924-1004`; explicit-limits overload = `cpp:1029-1135` | CONFIRMED | Both ranges verified exact (signature line to closing brace) | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:596-615` | TYPE_A: `totgComputeTimeStamps` = `cpp:1137-1160`; `num_waypoints < 2` check at `cpp:1147-1151`; upstream's first `computeTimeStamps` call inside it (`cpp:1154`) discards its `bool` return value entirely, while the port propagates that first call's `Err` instead | CONFIRMED | All ranges verified exact; `cpp:1154` — `default_totg.computeTimeStamps(trajectory, ...)`, return value discarded as a statement-expression | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:671-686` | TYPE_A: `validateGroup` inlined at `cpp:931-936` per overload; `computeJointVariableIndices(...)` call at `cpp:945-946/1053` | CONFIRMED | Both citations verified exact | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:704-706,715-717` | TYPE_A: `verifyScalingFactor` = `cpp:1290-1312`; `hasMixedJointTypes` = `cpp:1273-1288` | CONFIRMED | Both spans verified exact (signature to closing brace) | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:733,740-742,762-763,768-786,832` | TYPE_B: `doTimeParameterizationCalculations` = `cpp:1162-1271`; several inline comments transcribed near-verbatim from upstream | CONFIRMED | Function boundary exact; comments verified verbatim/near-verbatim at `cpp:1166,1186-1187` (incl. the `tobiaskunz/trajectories/issues/3` link), `1193,1199,1209-1210,1253` | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:909-914` | TYPE_B: upstream's own `add_waypoint` lambda passes `delta_t` for every waypoint including the first; upstream's `RobotTrajectory` never enforces `duration_from_previous[0] == 0.0` | CONFIRMED | `test_time_optimal_trajectory_generation.cpp:389-401` (`testLastWaypoint`) — lambda always passes `0.1`, called at :393 for the first waypoint too; `robot_trajectory.hpp:198-203` — `addSuffixWayPoint` unconditionally `push_back`s `dt` with no check on index 0 | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:927-939` | TYPE_B: `upstream_test_custom_limits` reproduces upstream `testCustomLimits` (test file lines 209-240): same fixture positions/limits, same "mutate `JointModel` bounds after construction via `setAccelerationLimits`" precedent | CONFIRMED | `test_time_optimal_trajectory_generation.cpp:209-240` matches exactly — same two joint-position arrays, same per-joint limits `{1.3,...,7.3}` | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:968-971` | TYPE_B: `verify_scaling_factor_boundaries` test — `(0.0,1.0]` passes through, else replaced by default | CONFIRMED | `cpp:1302` — exact match to the port's condition and the test's boundary assertions | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:1232-1247` | TYPE_B: empty trajectory is a silent no-op success (`cpp:928-929/1034-1035`); single-waypoint trajectory collapses to one zero-velocity/acceleration waypoint (`cpp:1218-1227`'s `points.size() == 1` branch) | CONFIRMED | Both cited line pairs verified exact | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:1273-1277,1302-1306` | TYPE_B: all-identical waypoints hit the same `points.size()==1` collapse via the diversity check (`cpp:1194-1216`); a middle duplicate waypoint is dropped via the `else if (p == num_points - 1)` branch never firing for a non-final index | CONFIRMED | `cpp:1194-1216` verified exact | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:1337-1345` | TYPE_B (repo-internal fixture claim): PR2 `r_end_effector` has 4 active joints, 7 total variables (3 mimic joints of `r_gripper_l_finger_joint`) | CONFIRMED | `fixtures/pr2.urdf` — exactly 3 `<mimic joint="r_gripper_l_finger_joint" .../>` entries | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:1387-1392` | TYPE_A: a custom limit-map entry is never bounds-checked, unlike the `RobotModel` fallback branch | CONFIRMED | `cpp:1076-1088/1110-1122` read and verified exact, matching the earlier-confirmed no-validation ranges | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:1421-1427` | TYPE_B: a zero custom acceleration limit still fails downstream inside `Trajectory::create`, matching upstream's `testRelevantZeroMaxAccelerationsInvalidateTrajectory` | CONFIRMED | `test_time_optimal_trajectory_generation.cpp:613-621` — test exists with that exact name and assertion | (uncommitted) |
| `src/time_optimal_trajectory_generation.rs:1436-1439` | TYPE_B: `panda_arm` all-revolute; PR2 `r_end_effector` mixes revolute (`r_gripper_l_finger_joint`, `r_gripper_motor_screw_joint`) and prismatic (`r_gripper_motor_slider_joint`, `r_gripper_joint`) | CONFIRMED (required extra verification) | `r_gripper_motor_screw_joint` is URDF type `continuous`, not literally `revolute`, but upstream's `robot_model.cpp:944-961` constructs URDF `CONTINUOUS` joints as `RevoluteJointModel` with `setContinuous(true)`, and `RevoluteJointModel`'s ctor unconditionally sets `type_ = REVOLUTE` (`revolute_joint_model.cpp:58`), so `getType()` returns `REVOLUTE` for it — the port's `moveit-model/src/joint/urdf.rs:53-66` mirrors this exactly | (uncommitted) |
| `tests/large_accel.rs:5-7` | TYPE_B: ports `TEST(time_optimal_trajectory_generation, testLargeAccel)` | CONFIRMED | `test_time_optimal_trajectory_generation.cpp:243` — exactly `TEST(time_optimal_trajectory_generation, testLargeAccel)` | (uncommitted) |
| `tests/large_accel.rs:9-12` | TYPE_B: upstream samples `getAcceleration` across the whole duration of a 6-DOF trajectory, asserting every component stays within a generous bound | CONFIRMED | `test_time_optimal_trajectory_generation.cpp:309-319` — identical sample loop (`ceil(duration/resample_dt)`, `size()==6`, `EXPECT_NEAR(acceleration(i), 0.0, 100.0)`); port uses strict `<` vs upstream's `EXPECT_NEAR`, immaterial to the claim | (uncommitted) |
| `tests/large_accel.rs:14-19`, fixture `.json:20` | TYPE_B: fixture holds "upstream's own test data verbatim, extracted mechanically... rather than retyped by hand"; cites `test_time_optimal_trajectory_generation.cpp:243-320` | CONFIRMED | Line range 243-320 verified exact; every waypoint/`max_velocity`/`max_acceleration` literal diffed as an `f64` bit pattern (not string) against the upstream `.cpp` literal (lines 254-299) — all bit-identical despite different printed digit counts (expected from `serde_json`'s shortest round-trip formatting) | (uncommitted) |
| `tests/large_accel.rs:20-24` | TYPE_B: `0.78539816339699997` is "4550 ULPs" from `FRAC_PI_4`, "not equal and not one ULP apart" | **EXPIRED** | Verified with both a Python bit comparison and a compiled Rust program using `f64::to_bits()`/`std::f64::consts::FRAC_PI_4`: the actual distance is **4038 ULPs**, not 4550 (same figure and same defect as `trajectory.rs:1302-1309` above — the wrong number is duplicated in both places). The qualitative claim ("not equal, not adjacent") is still true, only the cited magnitude is wrong | (uncommitted) |
| `tests/ruckig_smoothing.rs:5-6,8` | TYPE_B: ports `test_ruckig_traj_smoothing.cpp`; "every upstream `RuckigTests` case is ported" | CONFIRMED | File exists at that path in the pinned commit; `rg '^TEST_F\(RuckigTests'` finds exactly 4 cases, all 4 have a corresponding Rust `#[test]` | (uncommitted) |
| `tests/ruckig_smoothing.rs:8-11` | TYPE_A: `zeroVelocities()`/`zeroAccelerations()` omitted because "a freshly constructed `RobotState` already starts with all-zero velocity/acceleration storage... so there is nothing to zero" | **EXPIRED (correct conclusion, wrong mechanism)** | `robot_state.cpp:222-232` — upstream's `zeroVelocities()`/`zeroAccelerations()` do **not** write zeros into storage; they set `has_velocity_`/`has_acceleration_ = false` (a presence flag) and call `markVelocity()`/`markAcceleration()`. `robot_state.cpp:66-70` — upstream's own constructor already initializes `has_velocity_(false)`/`has_acceleration_(false)`, so upstream's own test calls are themselves redundant on a fresh state. The port's `RobotState::new` (`moveit-state/src/state.rs:218-222`) also defaults both presence flags to `false`, so omitting the calls is safe in practice — but the claimed reason ("storage already zero") misdescribes upstream's actual mechanism, which is a presence flag, not zeroed storage | (uncommitted) |
| `tests/ruckig_smoothing.rs:12-15` | TYPE_A: `single_waypoint`'s waypoint uses dt `0.0`, not upstream's `DEFAULT_TIMESTEP`; upstream's value would be `Err` under this port's `duration_from_previous[0]==0.0` invariant | CONFIRMED | `test_ruckig_traj_smoothing.cpp:152` — `addSuffixWayPoint(robot_state, DEFAULT_TIMESTEP)` (=0.1); port's `single_waypoint_trajectory_is_unmodified` uses `0.0`; `tests/robot_trajectory.rs`'s `add_suffix_way_point_on_an_empty_trajectory_rejects_a_nonzero_dt` confirms the rejection | (uncommitted) |
| `tests/ruckig_smoothing.rs:96-101,118-121,128-130,148-159` | TYPE_B: `IDEAL_DURATION = 0.210`, joint-0-only-moves comment, duration-bound factors `0.9999`/`1.11` ("extends duration by 10% at every iteration"), literal per-joint limits | CONFIRMED | `test_ruckig_traj_smoothing.cpp:110-113,119-121,136-139,101-103` all match byte-identically/near-verbatim; `ruckig_traj_smoothing.cpp:53-54,268-323` confirms `DURATION_EXTENSION_FRACTION = 1.1` applied multiplicatively in the retry loop | (uncommitted) |
| `tests/robot_trajectory.rs:5-14` | TYPE_B: enumerated not-ported/adapted set — `RobotTrajectoryShallowCopy` skipped; `ChainEdits`/`DoubleReverse` adapted (msg round-trip dropped); `MultiDofTrajectoryToJointStates`, `SetMultiDofTrajectory` skipped | CONFIRMED | Read the header directly: all 21 `TEST_F` cases in `test_robot_trajectory.cpp` enumerated; 18 map 1:1 to Rust tests (incl. the 2 adapted); the 3 named-skipped are exactly the ones absent | (uncommitted) |
| `tests/robot_trajectory.rs:58,72-77` | TYPE_A: `init_test_trajectory` = `RobotTrajectoryTestFixture::initTestTrajectory`; upstream's fixture pushes `duration_from_previous` (0.1) unconditionally, including at waypoint 0 | CONFIRMED | `test_robot_trajectory.cpp:80-89` — `addSuffixWayPoint(*robot_state_, duration_from_previous)` called identically for `ix=0..5` with `duration_from_previous=0.1` fixed, no special-case for `ix==0`; `EXPECT_EQ(getDuration(), 0.1*5)` = 0.5, matching | (uncommitted) |
| `tests/robot_trajectory.rs:334-345` | TYPE_B: `find_way_point_indices_between_waypoints` ports `RobotTrajectoryFindWayPointIndicesBetweenWaypoints` | **EXPIRED (undisclosed partial port)** | `test_robot_trajectory.cpp:602-622` runs **two** assertion blocks (`duration=0.15`→before=0,after=1,blend≈0.5; `duration=0.3`→before=1,after=2,blend≈1.0); the Rust test only ports one query, and the header's explicit not-ported list (`tests/robot_trajectory.rs:8-14`) does not disclose this partial-coverage gap within an otherwise-claimed "ported" test | (uncommitted) |
| `tests/robot_trajectory.rs:355-358` | TYPE_A: "upstream's own `EXPECT_DOUBLE_EQ` tolerates the same few-ULP rounding this assertion allows for" (port uses `< 1e-9`) | **EXPIRED (magnitude mismatch)** | gtest's `AlmostEquals`/`kMaxUlps = 4` is ULP-based (~2.2e-16 near 1.0); reproducing the exact upstream algorithm (`robot_trajectory.cpp:582-599`) with the fixture's actual dt sequence `[0,0.1,0.1,0.1,0.1]` gives `blend - 1.0 == -2.220446049250313e-16`, exactly 1 ULP. The port's chosen tolerance (`1e-9`) is ~7 orders of magnitude looser than both the actual few-ULP error and gtest's 4-ULP tolerance — "tolerates the same" overstates the equivalence, even though the underlying subtraction expression is verified byte-identical to upstream `robot_trajectory.cpp:598` | (uncommitted) |
| `tests/robot_trajectory.rs:674-681` | TYPE_A: upstream's `addSuffixWayPoint`/`addPrefixWayPoint`/`insertWayPoint` call `state->update()` before storing | CONFIRMED | `robot_trajectory.hpp:198-229` — all three overloads call `state->update();` as their first statement before pushing into `waypoints_` | (uncommitted) |
| `tests/robot_trajectory.rs:412-437,457` | TYPE_B: `random_large_angle = 20.2` unwinds to `1.350444` rad; `wrapped_angle = base + 12.566371` (`≈4π`) | CONFIRMED | `test_robot_trajectory.cpp:716,720,734` — identical literals; independently verified `20.2 - 3·2π = 1.3504441...` | (uncommitted) |
| `src/robot_trajectory.rs:31-37` (cited by `tests/robot_trajectory.rs:9,730-734`) | TYPE_A: upstream's `RobotTrajectory(model, group)` calls `getJointModelGroup`, which logs and returns `nullptr` for an unknown name — trajectory silently becomes whole-robot | CONFIRMED (duplicate of `lib.rs:374-381` row above, independently re-confirmed by this second agent) | `robot_trajectory.cpp:65`; `robot_model.cpp:512-521` — `RCLCPP_ERROR` + `return nullptr`; `robot_trajectory.cpp:88-94` — `getGroupName()` returns empty string when `group_` is null | (uncommitted) |
| `tests/fixtures/totg_synthetic.urdf:18-22` | TYPE_B: "moveit-model's URDF loader never reads bounds for a multi-DOF joint... matching upstream's `RobotModel::buildJointInfo`, which likewise never calls `jointBoundsFromURDF`... for planar/floating joints" | CONFIRMED (fixture consumed by `totg_synthetic_parity.rs`/`totg_robot_trajectory_parity.rs`, outside this file's primary scope, checked per instruction to look "briefly") | `robot_model.cpp:895-925` (`jointBoundsFromURDF`) is only invoked from `constructJointModel`'s `REVOLUTE`/`CONTINUOUS`/`PRISMATIC` cases (lines 947,956,965); the `FLOATING`/`PLANAR` cases never call it | (uncommitted) |

## Cross-crate finding — not fixed, `moveit-planning` is `p1-fixtures`' crate

`crates/moveit-planning/src/response_adapters/add_time_optimal_parameterization.rs:91`
(`AddTimeOptimalParameterization::new`) stores its `resample_dt: f64` argument
with no validation, and `:117` passes that unvalidated value straight into
[`apply_totg_time_parameterization`](../../crates/moveit-trajectory/src/trajectory_tools.rs)
at `adapt()`'s call site. This is the same §172-family dual-meaning problem
`TotgOptions::resample_dt` had before this round's structural fix
(construction-time value, no invariant enforced until first use) — one crate
over, now that `TotgOptions` itself can no longer hold an invalid
`resample_dt`. It is not a live hang: `apply_totg_time_parameterization`
calls `TotgOptions { .. }.with_resample_dt(resample_dt)?` (added this round),
so an invalid value from `AddTimeOptimalParameterization::new` surfaces as an
`Err` at `adapt()` time, not silent truncation or a hang. But
`AddTimeOptimalParameterization`'s own public constructor still accepts and
stores the invalid value before that point, which is the defect: a caller
building the adapter cannot tell it is invalid until `adapt()` runs.

Per `doc/crate-ownership.md`, `moveit-planning` belongs to `p1-fixtures`, not
`p6-totg`. **Not fixed here** — reported so it is not lost; `p1-fixtures`
should apply the same validate-at-construction structural fix (or route
`AddTimeOptimalParameterization::new` through `TotgOptions::with_resample_dt`
and store the validated `TotgOptions` instead of three loose `f64` fields).

## Background-agent audit — lost to compaction, superseded by the re-run above

An earlier background agent in this session (Item 4 of the governing
task: "audit upstream claim used as basis, type-b vs type-a") reported
**3 claimed TYPE_B findings and 7 claimed TYPE_A findings out of 60
candidates examined** across this crate, `moveit-planners-chomp`, and
`moveit-smoothing`. The itemized list (file:line, claim text, proposed
verdict) was in conversation context only and did not survive a
subsequent context compaction — exactly the failure mode §175
describes. No item from that list was ever recorded here as
`CONFIRMED` or `EXPIRED`; the surviving aggregate counts ("3 TYPE_B, 7
TYPE_A") were never usable as evidence without the underlying
file:line/claim/evidence triple.

**Superseded, not merely UNVERIFIABLE:** the table above this section
is a full fresh re-run of the same audit (this crate's own claims,
184 rows), appended row-by-row as found rather than batched, closing
the gap this section originally flagged. The lost aggregate counts
above are not reconciled against the fresh run's counts (166
CONFIRMED / 14 EXPIRED / 5 UNVERIFIABLE) — they were never granular
enough to reconcile against anything — so this section is kept only
as a record of the failure mode, not as an open item.
