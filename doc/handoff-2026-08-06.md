# Handoff — 2026-08-06

Written mid-round at the user's request (session limit at 99%). Everything
below is the state of `main` at `13e39a9` plus what is in flight elsewhere.

## main

`13e39a9`, **22+ commits ahead of `origin/main`** (`c178722`). The one
authorized push landed `c178722`; **no push has been authorized since**.

Gate state measured on `main` this session:

| gate | result |
| --- | --- |
| `cargo fmt --all` | pass |
| `cargo clippy --workspace --all-targets -- -D warnings` | pass |
| `cargo nextest run --workspace` | **1987/1987 passed**, 3 skipped |
| `cargo test --doc --workspace` | pass |
| `check-citation-drift.py` | pass (baseline re-frozen at `766c52e`) |
| the 16 `check-*.sh` | pass |
| `verify-declaration-audits.sh`, `verify-private-doc-links.sh`, `verify-port-coverage.sh`, `verify-upstream-citations.sh`, `verify-client-endpoint-surface.sh`, `verify-vendored-fixture-tests.sh` | pass |
| `verify-orphan-enumeration.sh` | **FAIL — 22 orphans, 0 unresolved citations** (owned, see below) |

Not re-run after this session's last commit, and therefore **unverified**:
`verify-clean-checkout.sh`, and every docker gate
(`verify-ros-interop.sh`, `verify-move-action-interop.sh`,
`verify-oracle-sweep.sh`, `verify-phase3-collision-sweep.sh`,
`verify-phase7-benchmark.sh`, `verify-phase8-benchmark.sh`,
`verify-fixture-replay.sh`). `13e39a9` edits all of them. Run those first.

## What landed on main this session

- `2250057` — merged p9-ros's `planning.rs` start_state ledger row.
- `9e6873a` — that row's `**Totals:**` line said 70 examined / 68 in-family
  / 4 not-family, which does not close (68+4 = 72). Enumerating the table's
  own fourteen rows gives **73 / 69 / 4**. The base was already one low: an
  earlier round widened `planning.rs:773,784,817,1046` from three sites to
  four ("was 3") without bumping the total.
- `766c52e` — re-froze `doc/citation-classes.txt`: 2403 → 2411 citations,
  8 new keys, **0 demoted, 0 retired**.
- `13e39a9` — the caller-tree guard, below.

## `13e39a9` — the gate-tree guard, and why it matters to every worker report

p11-scenetopic reported that
`sg docker -c '/home/stevek/work/moveit-rs/tools/ci/verify-ros-interop.sh'`
— the exact command my own briefs prescribe — printed `all gates passed`,
exit 0, 174/174 tests, **against a tree containing none of its changes**.
Every gate derives its root from `dirname "${BASH_SOURCE[0]}"`, so an
absolute invocation gates the script's own checkout, not the caller's. The
docker gates must be invoked absolutely because `sg docker -c` takes a
command string and not a working directory.

**Anchor:** `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/…`
**Sites:** 47 (44 var-shaped, 3 `cd`-shaped, 1 `HERE`-shaped in
`tools/mpr-vs-epa/build.sh`).
**Same defect at:** all 47 — all fixed in `13e39a9`.
**Distinct, skip:** none.

`require_caller_tree` in `tools/ci/gate-lib.sh` fails only where it can
prove the mistake: caller and gate in two worktrees sharing one `.git`.
An unrelated repository, or no repository, still passes — so release
tarballs and `verify-clean-checkout.sh`'s `--local` clone are unaffected.

Verified: no-op from the repo root by both relative and absolute path;
fires from a caucus worktree with the correct invocation printed.

**Consequence for anything already reported.** Any docker-gate result a
worker produced by absolute path before `13e39a9` measured the session
root, not its branch. Those greens carry no information about the branch
they were reported for and must be re-run from the worktree's own copy.

One process note found while doing this: my first ordering check reported
"0 files with the call before the source" because its regex `[^"]*` could
not span the nested quotes in `. "$(dirname "${BASH_SOURCE[0]}")/gate-lib.sh"`.
The real answer was 8. The checker failed toward silence — the same shape
the guard itself exists to prevent.

## In flight — 13 caucus panels, none merged

Only two branches carry unmerged commits; the rest were dispatched this
round and have not reported.

| panel | task | branch state |
| --- | --- | --- |
| p10-cachedik | Phase 9 D8: one request/response type per planner, both endpoints reaching a real planner, ros interop gate green end-to-end | **14 commits unmerged**, merge as a new `§` |
| p10-phase13 | close the remaining 22 orphans in `ros/moveit-ros`; sole owner of `doc/assertion-discrimination-ledger-p9-ros.md` | working |
| p10-gapaudit | fix 15 miscited evidence lines in `doc/port-coverage.md`; decide `GreedyKCenters.hpp` / `NearestNeighbors.hpp`; re-derive the 40-vs-35 discrepancy; commit `indep.py` | working |
| p10-phase5 | measure upstream C++ CHOMP/STOMP on the same 500 problems | working |
| p3-acm | is the 6,854-sample `collision: bool` divergence one degenerate contact? | working |
| p9-ros | gate every ledger's `**Totals:**` against its own table | working |
| p10-samplers | detect silent section deletions across the 62 invisible documents | working |
| p10-phase7 | check §5's twenty verdict rows against the sections they cite | working |
| p10-cartesian | bind `compute_cartesian_path` | working |
| p11-startstate | bind the `execute_trajectory` action server | working |
| p11-planningfailed | bind `query_planner_interface` / `get_planner_params` / `set_planner_params` | working |
| p11-scenetopic | subscribe `attached_collision_object` and `trajectory_execution_event` | working |
| p10-jointlimits | provide `robot_description` and `joint_states` | working |

Collect with `mcp__caucus__collect_workers`. **Reports re-deliver stale:**
one collection this session returned nine reports of which eight were
already merged. Check `git rev-list --left-right --count main...<branch>`
before acting on any of them.

## The three UNMET rows in §5 (`PORTING-PLAN.md:800-819`)

1. **Phase 3, `collision: bool` 100%** (§229.1). §229.1 settled that there
   is no single upstream convention at exact tangency — two pairs answer
   oppositely. The open question I gave p3-acm is different: exact `z = 0`
   tangency has probability zero under continuous sampling, so 6,854 of
   10,000 samples hitting it points at the fixture, not the sweep. If the
   same link pair diverges every time, this is one geometric fact wearing
   6,854 hats and the row is measurable on a non-degenerate scene.
2. **Phase 8, CHOMP/STOMP** (§263). Measured UNMET: CHOMP 380/500 = 76.0%,
   STOMP 441/500 = 88.2%, bar 89.64%. But the bar is `0.9 × 498/500` where
   498/500 is **C++ OMPL RRTConnect** — a different planner. p10-phase5 is
   measuring upstream's own C++ CHOMP/STOMP on the identical 500 problems
   to decide whether UNMET means a port defect or a mis-specified criterion.
3. **Phase 9, unmodified C++ `MoveGroupInterface`** (§250.5). p10-cartesian's
   surface derivation is the work plan: 13 endpoints, **1 bound, 3 surplus,
   9 absent**. The 9 are fanned across five panels (table above). Upstream
   sources are on this machine at
   `/home/stevek/work/moveit2/moveit_ros/move_group/src/default_capabilities/`.

Phase 3's `distance` row is PARTIAL against §260.

## Standing rules for the next session

- **Never push without explicit confirmation.** Main is 22+ ahead of origin.
- Workers never push, merge, or rebase; only the merging agent merges.
- Do not let a worker re-freeze `doc/citation-classes.txt` — the merger owns
  it, and the gate now fails on a *demotion*, so a worker absorbing one
  hides drift.
- `sg docker -c '<absolute path>'` for docker, always — and after `13e39a9`
  it must be the **worktree's own** absolute path.
- Section numbers: workers write `## §NEW` / `### §NEW.N`; the merger
  assigns numbers. Highest assigned is **§264**.
- Off-limits pairs currently live: `ledger-p9-ros.md` (p10-phase13),
  `doc/port-coverage.md` (p10-gapaudit), `doc/client-endpoint-surface.md`
  and §259 (p10-cartesian), `doc/claim-audit/tools-ci-gates.md`
  (p10-samplers).
- One commit per finding.

## Owed, not done

- Re-run `verify-clean-checkout.sh` and all seven docker gates against
  `13e39a9` — they were edited and not re-measured.
- p9-ros owes `doc/claim-audit/tools-ci-gates.md` the anchor form that
  defeats `check-citation-drift.py`: a qualified path such as
  `TryFrom<PlanningRequestMsg>::try_from`, not a bare snake_case
  identifier. It is writing it as a plan section for p10-samplers to route.
- p10-samplers' branch carries `ac4bcfa`, an amended duplicate of the
  already-merged `f0aa8fe`; content-identical, harmless, worth dropping at
  its next merge.
- p10-cartesian's `strip_rust` block-comment branch executes 0 times on
  today's corpus. It was bitten with an inserted comment rather than
  shipped untested; no tracked file exercises it.
