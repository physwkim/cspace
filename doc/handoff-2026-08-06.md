# Handoff — 2026-08-06

State of `main` at `2ab7a90`, plus what is in flight elsewhere. Supersedes
the `61c50ca` revision of this file.

## main

`2ab7a90`, **59 commits ahead of `origin/main`** (`c178722`). The one
authorized push landed `c178722`; **no push has been authorized since**.

Gate state, each measured today with its own exit status read directly —
not through a pipe, because `gate | tail -5` reports `tail`'s status and
that is how two of the failures below stayed invisible:

| gate | result |
| --- | --- |
| the 16 `check-*.sh` | pass |
| `check-citation-drift.py` | pass (re-frozen at `c2133fb`) |
| `verify-private-doc-links.sh`, `verify-upstream-citations.sh`, `verify-client-endpoint-surface.sh`, `verify-vendored-fixture-tests.sh` | pass |
| `verify-port-coverage.sh` | **FAIL — 1 stale row**, fixed on p10-gapaudit's branch (`c74704e`) |
| `verify-declaration-audits.sh` | **FAIL — 1 missing row**, routed to p10-cachedik |
| `verify-orphan-enumeration.sh` | **FAIL — 22 orphans, 0 unresolved**, owned by p10-phase13 |

`cargo fmt --all` clean. `clippy`/`nextest` were run at `-p
moveit-planners-chomp -p moveit-planners-stomp` for `2ab7a90` (148/148);
**the full-workspace pass is still owed** and is a precondition for any
push. So are `verify-clean-checkout.sh` and the seven docker gates after
`13e39a9`.

## What landed on main since `61c50ca`

- `d030186` — two gates named `gate-lib.sh` only in a prose comment, so
  the applier that wired in `require_caller_tree` skipped them and the
  call was an undefined command that printed `command not found` and let
  the run continue. Re-verified by execution this time: all 48 guarded
  scripts, invoked from a caucus worktree, refuse. 48 fire, 0 don't.
- `6c51a55` — `crates/moveit-planning/src/start_state.rs` asserted a 2012
  Willow Garage notice that none of its four cited upstream files carries.
  Replaced with the four they do carry.
- `c2133fb` — and that header edit was net **+3 lines**, so every citation
  into the file was three short. See below; this is the entry the next
  session should read first.
- `2ab7a90` — `44843b2` added the two Phase 8 property examples without an
  SPDX header, so `check-license-matches-upstream.sh` had been red on main
  since. Both now carry the "no upstream file" note their `*_benchmark_port`
  siblings use.

## The citation-shift family, and why the gate saw 2 of 55

`6c51a55` shifted `start_state.rs` by +3. `verify-orphan-enumeration.sh`
went from 22/0 to **24 orphans / 2 unresolved citations**;
`check-citation-drift.py` reported exactly **two** demotions,
`:327` and `:357`. Those two are the file's only *anchor-verified*
citations. The other 53 sat in the content-verified and unanchored
buckets, where a wrong line number inside a real file passes a bounds
check silently.

All 55 were re-derived through `difflib.SequenceMatcher` against
`git show 6c51a55^:<file>` and **each endpoint confirmed by its own
content** — never by adding three. Four more, in
`doc/assertion-discrimination-ledger-p9-ros.md`, were routed to
p10-phase13 rather than fixed here, because that panel owns the file.

**The structural cause is measured and open as task #15.** The drift
gate's corpus is backticked `` `path.rs:NNN[-MMM]` `` spans only. The
*shorthand* form — a backticked `` `:NNN` `` whose file is named earlier
in the prose — is in no gate's corpus at all: not bounds, not anchor, not
content. There are **1,940** of them across tracked `.md` files
(PORTING-PLAN.md 522, `ledger-p1-robotmodel` 234, `doc/upstream-bugs.md`
198, `claim-audit/moveit-scene` 154, `ledger-p9-ros` 111). A naive
"exactly one `.rs` path on the same line" rule resolves 566, plus 252
more from the enclosing paragraph; **1,122 do not resolve**, because the
governing path usually sits several paragraphs up under the section
heading. A stronger rule to try: scope to the enclosing markdown section,
resolve to the nearest *preceding* full-path citation, and accept only if
the line is in bounds for that file.

## In flight — five panels, the cap the user set

| panel | task | commits |
| --- | --- | --- |
| p10-phase13 | the 22 orphans in `ros/moveit-ros`; owns `ledger-p9-ros.md` | 8 |
| p10-cachedik | the §5 Phase 9 row, plus the `planning_interface.hpp` audit row | 5 |
| p10-gapaudit | the 15 miscitations in `doc/port-coverage.md` | 3 |
| p10-phase5 | upstream C++ CHOMP/STOMP on the same 500 problems | 2 |
| p10-samplers | the silent-section-deletion detector | 0 |

Eleven further panels are alive but idle with **no commits** — they lost
their round to the session reset. Restarting one costs a slot, so the cap
of five is the real constraint, not the panel count.

Parked with no owner: the five Phase 9 endpoint bindings
(`execute_trajectory`, `robot_description`+`joint_states`,
`compute_cartesian_path`, the three planner-params services, the two
inbound topics), the ledger-totals gate (#8), the §5 verdict-row check
(#13), and the shorthand-citation corpus (#15).

## The three open rows in §5 (`PORTING-PLAN.md:801-819`)

The table is **19 rows**, not the twenty an earlier note claimed; it runs
from the header at `:799` to the blank line at `:820`.

1. **Phase 3, `collision: bool` — UNMET, cited §229.1.** §265 settled the
   cause: all 6,854 disagreements are the single pair
   `floor`/`prbt_base_link`, whose world pose is joint-independent, so its
   cylinder bottom meets the harness floor's `z = 0` in every sample. But
   §265.7 states plainly that it re-ran **prbt alone** and quoted the other
   four robots from a previous round, and it assigns the 5-robot re-run on
   merged main to the merger. That run is in flight; **panda 0/10000 and
   prbt 6854/10000 are in hand and reproduce §265 exactly**, fanuc /
   dual_arm_panda / pr2 outstanding. The wording §265.7 proposes, confirmed
   unchanged from §262.2, is *"10,000×5로봇에서 100% 일치; 정확 접선 좌표는
   제외 — 상류 자체에 규약이 없음"*. Note the row must **not** cite §265 for
   it: §265 says it did not make that measurement. It needs a new section.
2. **Phase 8, CHOMP/STOMP — UNMET, cited §263.** The port measured CHOMP
   380/500 and STOMP 441/500 against a 89.64% bar that is `0.9 × 498/500`
   where 498/500 is **C++ OMPL RRTConnect**, a different planner.
   p10-phase5 has upstream's own C++ CHOMP and STOMP running at the pinned
   SHA and is sweeping the identical 500. Its strongest result so far is
   that cage/221 is **not** a port defect: `ChompPlanner::solve` gates
   SUCCESS on `optimizer->isCollisionFree()` over its own 101 points and
   nothing in CHOMP inspects between waypoints, so the criterion's 0.01
   densification tests something upstream never validates.
3. **Phase 9, unmodified C++ `MoveGroupInterface` — UNMET, cited §250.5.**
   §266 (D8) closes with the condition met; the row still says UNMET.
   p10-cachedik is settling it and has both `/plan_kinematic_path` and
   `/move_action` planning over DDS, with leg B (the upstream C++ client)
   still returning `-16` on `start_state`. Independently,
   `doc/client-endpoint-surface.md` derives 13 endpoints: **1 bound, 3
   surplus, 9 absent**.

Phase 3's `distance` row is PARTIAL against §260, and the sweep's
separated branch (`oracle value > 0`) is 0/8734 on panda and 0/9611 on
prbt so far, consistent with §260.2.

## Standing rules

- **Never push without explicit confirmation.** Main is 59 ahead.
- Workers never push, merge, or rebase; only the merging agent merges.
- The merger owns `doc/citation-classes.txt`. The gate fails on a
  *demotion*, so a worker re-freezing over one hides drift.
- `sg docker -c '<the worktree's own absolute path>'`, always. After
  `13e39a9` an absolute path into the session root refuses rather than
  silently gating the wrong tree — but any docker green a worker reported
  **before** `13e39a9` measured the session root and carries no
  information about its branch.
- Read a gate's own exit status. `gate | tail -N` reports `tail`'s.
- Workers write the unassigned-section placeholder; the merger assigns the
  number. Highest assigned is **§266**.
- Off-limits pairs currently live: `ledger-p9-ros.md` (p10-phase13),
  `doc/port-coverage.md` (p10-gapaudit), `doc/client-endpoint-surface.md`
  and §259 (p10-cartesian), `doc/claim-audit/tools-ci-gates.md`
  (p10-samplers).
- One commit per finding.
- Collect with `mcp__caucus__collect_workers`, but **reports re-deliver
  stale** — one collection returned nine reports of which eight were
  already merged. Check `git rev-list --left-right --count main...<branch>`
  first.

## Owed, not done

- The full-workspace `clippy`/`nextest`/`test --doc` pass, and
  `verify-clean-checkout.sh` plus the seven docker gates against
  `13e39a9`. All are push preconditions.
- p9-ros owes `doc/claim-audit/tools-ci-gates.md` an anchor form that
  defeats `check-citation-drift.py`: a qualified path such as
  `TryFrom<PlanningRequestMsg>::try_from`, not a bare snake_case
  identifier.
- p10-samplers' branch carries `ac4bcfa`, an amended duplicate of the
  merged `f0aa8fe`; content-identical, worth dropping at its next merge.
- p10-cartesian's `strip_rust` block-comment branch executes 0 times on
  today's corpus — bitten with an inserted comment, never exercised by a
  tracked file.
